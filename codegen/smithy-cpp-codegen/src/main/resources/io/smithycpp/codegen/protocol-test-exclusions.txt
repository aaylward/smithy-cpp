# Protocol test exclusion list (smithy-rs FailingTest pattern). One entry per
# line: <service shape id> <request|response|error|any> <test id> <reason>.
# Every entry must match a test that would otherwise be generated — stale
# entries fail generation — and the list must only shrink over time.

# @default / @clientOptional value population is not implemented yet.
smithy.protocoltests.rpcv2Cbor#RpcV2Protocol request RpcV2CborClientPopulatesDefaultValuesInInput @default population is not implemented yet
smithy.protocoltests.rpcv2Cbor#RpcV2Protocol response RpcV2CborClientPopulatesDefaultsValuesWhenMissingInResponse @default population is not implemented yet

# Generated output comparison uses operator==, under which NaN != NaN; the
# client deserializes these payloads correctly but the assertion cannot pass.
smithy.protocoltests.rpcv2Cbor#RpcV2Protocol response RpcV2CborSupportsNaNFloatOutputs NaN output members compare unequal under operator==
smithy.protocoltests.rpcv2Cbor#RpcV2Protocol response RpcV2CborFloat16LSBNaN NaN output members compare unequal under operator==
smithy.protocoltests.rpcv2Cbor#RpcV2Protocol response RpcV2CborFloat16MSBNaN NaN output members compare unequal under operator==

# Quoted-string list headers need RFC 9110 quoting/unquoting.

# @httpChecksumRequired (Content-MD5) is not implemented yet.


# The test asserts an all-zero UUID, which needs an injectable token source.

# Server-mode exclusions (Phase 4b).
# @default population is not implemented yet (server side).
smithy.protocoltests.rpcv2Cbor#RpcV2Protocol server-request RpcV2CborServerPopulatesDefaultsWhenMissingInRequestBody @default population is not implemented yet
smithy.protocoltests.rpcv2Cbor#RpcV2Protocol server-response RpcV2CborServerPopulatesDefaultsInResponseWhenMissingInParams @default population is not implemented yet

# Parsed-input comparison uses operator==, under which NaN != NaN.
smithy.protocoltests.rpcv2Cbor#RpcV2Protocol server-request RpcV2CborSupportsNaNFloatInputs NaN input members compare unequal under operator==

# These cases define no wire body (client-only semantics: they assert the
# Content-Encoding header the client sends), so a server cannot parse the
# expected params from them.

# Absent query list params deserialize as unset members, not engaged empty lists.

# std::regex is a backtracking engine, so the deliberately catastrophic
# pattern ^([0-9]+)+$ hangs; ReDoS-safe @pattern matching needs a linear-time
# regex engine (tracked in PLAN).

# Nested @required absences (inside collections/structures) surface as strict
# deserialization errors without JSON-pointer paths; reporting them as fieldList
# entries needs a validating deserializer (PLAN follow-up).

# Clients and servers share one serde: clients must SKIP null dense-map values
# and ACCEPT RFC3339 UTC offsets in responses, so the server cannot also
# reject them in bodies until a server-strict serde variant lands (PLAN).

# These two contradict server-mode httpRequestTests in the same suite
# (RestJsonEndpointTraitWithHostLabel sends a body with no content-type and
# must parse; RestJsonEmptyInputAndEmptyOutputWithJson sends application/json
# to an empty-input operation and must succeed).

# simpleRestJson (alloy) conformance exclusions.
# The "None" payload-with-default variants exercise @default population: the
# client must parse an absent payload as the default (response), and the
# server must parse an absent request payload as the default (server-request).
# The other kinds carry no body assertion and pass without @default.
alloy.test#PizzaAdminService response SimpleRestJsonNoneRequiredHttpPayloadWithDefault @default population is not implemented yet
alloy.test#PizzaAdminService server-request SimpleRestJsonNoneRequiredHttpPayloadWithDefault @default population is not implemented yet
alloy.test#PizzaAdminService response SimpleRestJsonNoneHttpPayloadWithDefault @default population is not implemented yet
alloy.test#PizzaAdminService server-request SimpleRestJsonNoneHttpPayloadWithDefault @default population is not implemented yet

# alloy @discriminated / @untagged / open-union encodings are not implemented.
alloy.test#PizzaAdminService any OpenUnionsUnknownTaggedUnionCase alloy open/discriminated unions are not implemented
alloy.test#PizzaAdminService any OpenUnionsKnownDiscriminatedUnionCase alloy open/discriminated unions are not implemented
alloy.test#PizzaAdminService any OpenUnionsUnknownDiscriminatedUnionCase alloy open/discriminated unions are not implemented

# @httpResponseCode with a 3xx value: the client treats only 2xx as success.
alloy.test#PizzaAdminService response CustomCodeOutput 3xx @httpResponseCode is not treated as a success status

# Under investigation (task #63): un-exclude once diagnosed.
alloy.test#PizzaAdminService any GetEnumOutput under investigation (task #63): enum output server-side 400
alloy.test#PizzaAdminService any RoundTripRequest under investigation (task #63): mixed label/query/body echo
alloy.test#PizzaAdminService any RoundTripDataResponse under investigation (task #63): mixed label/query/body echo
