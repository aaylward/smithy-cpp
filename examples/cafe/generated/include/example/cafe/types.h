// Code generated by smithy-cpp (cpp-codegen). DO NOT EDIT.

#pragma once

#include <cstdint>
#include <optional>
#include <string>
#include <string_view>
#include <utility>
#include <variant>

#include "smithy/core/outcome.h"
#include "smithy/core/timestamp.h"

namespace example::cafe {

struct AlternativeMilk {
  std::string kind{};

  friend bool operator==(const AlternativeMilk&, const AlternativeMilk&) = default;
};


struct GetOrderInput {
  std::string orderId{};

  friend bool operator==(const GetOrderInput&, const GetOrderInput&) = default;
};


class CoffeeType {
  public:
    enum class Value {
      kDrip,
      kEspresso,
      kCortado,
      kLatte,
      kUnknown,
    };

    CoffeeType() = default;
    CoffeeType(Value value) : value_(value) {}  // NOLINT(*-explicit-*)

    /// Unknown wire values are preserved and reported as Value::kUnknown.
    static CoffeeType FromString(std::string_view text) {
      if (text == "DRIP") return CoffeeType(Value::kDrip);
      if (text == "ESPRESSO") return CoffeeType(Value::kEspresso);
      if (text == "CORTADO") return CoffeeType(Value::kCortado);
      if (text == "LATTE") return CoffeeType(Value::kLatte);
      CoffeeType result;
      result.unknown_ = std::string(text);
      return result;
    }

    Value value() const { return value_; }

    /// The wire text, including the original text of unknown values.
    std::string_view ToString() const {
      switch (value_) {
        case Value::kDrip: return "DRIP";
        case Value::kEspresso: return "ESPRESSO";
        case Value::kCortado: return "CORTADO";
        case Value::kLatte: return "LATTE";
        case Value::kUnknown: return unknown_;
      }
      return unknown_;
    }

    friend bool operator==(const CoffeeType&, const CoffeeType&) = default;

  private:
    Value value_ = Value::kUnknown;
    std::string unknown_;
};


struct CancelledStatus {
  std::optional<std::string> reason{};

  friend bool operator==(const CancelledStatus&, const CancelledStatus&) = default;
};


struct PendingStatus {
  std::int32_t position{};

  friend bool operator==(const PendingStatus&, const PendingStatus&) = default;
};


struct ReadyStatus {
  smithy::Timestamp readyAt{};

  friend bool operator==(const ReadyStatus&, const ReadyStatus&) = default;
};


class OrderStatus {
  public:
    OrderStatus() = default;

    static OrderStatus FromPending(PendingStatus value) {
      OrderStatus result;
      result.value_.emplace<1>(std::move(value));
      return result;
    }
    bool is_pending() const { return value_.index() == 1; }
    const PendingStatus& as_pending() const { return std::get<1>(value_); }

    static OrderStatus FromReady(ReadyStatus value) {
      OrderStatus result;
      result.value_.emplace<2>(std::move(value));
      return result;
    }
    bool is_ready() const { return value_.index() == 2; }
    const ReadyStatus& as_ready() const { return std::get<2>(value_); }

    static OrderStatus FromCancelled(CancelledStatus value) {
      OrderStatus result;
      result.value_.emplace<3>(std::move(value));
      return result;
    }
    bool is_cancelled() const { return value_.index() == 3; }
    const CancelledStatus& as_cancelled() const { return std::get<3>(value_); }

    /// True until one of the From* factories has been used.
    bool empty() const { return value_.index() == 0; }

    friend bool operator==(const OrderStatus&, const OrderStatus&) = default;

  private:
    std::variant<std::monostate, PendingStatus, ReadyStatus, CancelledStatus> value_;
};


struct GetOrderOutput {
  std::string orderId{};
  CoffeeType coffeeType{};
  OrderStatus status{};

  friend bool operator==(const GetOrderOutput&, const GetOrderOutput&) = default;
};


/// The order id does not correspond to any known order.
struct OrderNotFound {
  std::string orderId{};

  friend bool operator==(const OrderNotFound&, const OrderNotFound&) = default;
};


struct DairyMilk {
  float percentFat{};

  friend bool operator==(const DairyMilk&, const DairyMilk&) = default;
};


/// A union member per milk preparation, to exercise union codegen.
class MilkOption {
  public:
    MilkOption() = default;

    static MilkOption FromNone(smithy::Unit value) {
      MilkOption result;
      result.value_.emplace<1>(std::move(value));
      return result;
    }
    bool is_none() const { return value_.index() == 1; }
    const smithy::Unit& as_none() const { return std::get<1>(value_); }

    static MilkOption FromDairy(DairyMilk value) {
      MilkOption result;
      result.value_.emplace<2>(std::move(value));
      return result;
    }
    bool is_dairy() const { return value_.index() == 2; }
    const DairyMilk& as_dairy() const { return std::get<2>(value_); }

    static MilkOption FromAlternative(AlternativeMilk value) {
      MilkOption result;
      result.value_.emplace<3>(std::move(value));
      return result;
    }
    bool is_alternative() const { return value_.index() == 3; }
    const AlternativeMilk& as_alternative() const { return std::get<3>(value_); }

    /// True until one of the From* factories has been used.
    bool empty() const { return value_.index() == 0; }

    friend bool operator==(const MilkOption&, const MilkOption&) = default;

  private:
    std::variant<std::monostate, smithy::Unit, DairyMilk, AlternativeMilk> value_;
};


struct OrderCoffeeInput {
  CoffeeType coffeeType{};
  std::optional<MilkOption> milk{};
  std::optional<std::string> clientToken{};

  friend bool operator==(const OrderCoffeeInput&, const OrderCoffeeInput&) = default;
};


struct OrderCoffeeOutput {
  std::string orderId{};
  OrderStatus status{};

  friend bool operator==(const OrderCoffeeOutput&, const OrderCoffeeOutput&) = default;
};


/// The cafe cannot fulfill the order right now.
struct OutOfBeans {
  std::optional<std::string> message{};

  friend bool operator==(const OutOfBeans&, const OutOfBeans&) = default;
};

}  // namespace example::cafe
