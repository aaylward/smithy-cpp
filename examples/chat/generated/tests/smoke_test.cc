// Code generated by smithy-cpp (cpp-codegen). DO NOT EDIT.

#include <gtest/gtest.h>
#include <memory>
#include <utility>

#include "example/chat/client.h"
#include "example/chat/server.h"
#include "smithy/client/config.h"
#include "smithy/http/loopback.h"

namespace example::chat {

// Smoke tests for the generated Chat service: the generated client calls the
// generated server over the in-memory loopback transport. A passing suite
// pins routing, serde symmetry, required members, and error mapping; swap the
// stub handler for a real one to grow it into your service's test suite.

namespace {

ListRoomsOutput MinimalListRoomsOutput() {
    return [] {
    ListRoomsOutput v{};
    return v;
  }();
}

class SmokeHandler : public ChatHandler {
  public:
    // Streaming operation (ADR-0016): no generated unary-shaped test drives
    // this; the stub closes the stream so the interface stays implemented.
    smithy::Outcome<smithy::Unit> Converse(const ConverseInput& input, ConverseServerStream& stream, const smithy::server::RequestContext&) override {
      (void)input;
      stream.Close();
      return smithy::Unit{};
    }
    smithy::Outcome<ListRoomsOutput> ListRooms(const ListRoomsInput& input, const smithy::server::RequestContext&) override {
      (void)input;
      return MinimalListRoomsOutput();
    }
    // Streaming operation (ADR-0016): no generated unary-shaped test drives
    // this; the stub closes the stream so the interface stays implemented.
    smithy::Outcome<smithy::Unit> Watch(const WatchInput& input, WatchServerStream& stream, const smithy::server::RequestContext&) override {
      (void)input;
      stream.Close();
      return smithy::Unit{};
    }
};

ChatClient MakeClient(std::shared_ptr<ChatHandler> handler) {
  ChatServer server(std::move(handler));
  auto loopback = std::make_shared<smithy::http::Loopback>();
  (void)loopback->Start(server.Handler());
  smithy::ClientConfig config;
  config.retry.max_attempts = 1;  // wire-exact tests: no retries
  config.http_client = loopback;
  // Create cannot fail when a transport is injected.
  return *ChatClient::Create(std::move(config));
}

}  // namespace

TEST(ChatSmokeTest, ListRoomsRoundTrips) {
  ChatClient client = MakeClient(std::make_shared<SmokeHandler>());
    const ListRoomsInput input = [] {
    ListRoomsInput v{};
    return v;
  }();
  const auto outcome = client.ListRooms(input);
  ASSERT_TRUE(outcome.ok()) << outcome.error().message();
  EXPECT_EQ(*outcome, MinimalListRoomsOutput());
}

}  // namespace example::chat
