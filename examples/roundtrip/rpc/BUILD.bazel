load("@rules_cc//cc:defs.bzl", "cc_test")
load("//bazel:copts.bzl", "SMITHY_COPTS")

cc_test(
    name = "no_input_body_wire_test",
    size = "small",
    srcs = ["no_input_body_wire_test.cc"],
    copts = SMITHY_COPTS,
    deps = [
        "//examples/roundtrip/rpc/generated:server",
        "//runtime:http",
        "//runtime:protocol_test_support",
        "@googletest//:gtest_main",
    ],
)
