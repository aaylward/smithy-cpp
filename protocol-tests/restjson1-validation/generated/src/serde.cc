// Code generated by smithy-cpp (cpp-codegen). DO NOT EDIT.

#include <string>
#include <utility>

#include "smithy/core/document_serde.h"
#include "smithy/protocoltests/restjsonvalidation/serde.h"

namespace smithy::protocoltests::restjsonvalidation {

smithy::Document SerializeGreetingStruct(const GreetingStruct& value) {
  smithy::DocumentMap map;
  if (value.hi.has_value()) {
    map.emplace("hi", smithy::Document((*value.hi)));
  }
  return smithy::Document(std::move(map));
}

smithy::Outcome<GreetingStruct> DeserializeGreetingStruct(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("GreetingStruct: expected a map on the wire");
  GreetingStruct out;
  {
    const smithy::Document* member = doc.Find("hi");
    if (member != nullptr && !member->is_null()) {
      std::string parsed_member{};
      if (!member->is_string()) return smithy::Error::Serialization("GreetingStruct.hi: unexpected type on the wire");
      parsed_member = member->as_string();
      out.hi = std::move(parsed_member);
    }
  }
  return out;
}

smithy::Document SerializeStringList(const std::vector<std::string>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(smithy::Document(item));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<std::string>> DeserializeStringList(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<std::string>: expected a list on the wire");
  std::vector<std::string> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<std::string>: null element in a dense list");
    std::string parsed_item{};
    if (!item->is_string()) return smithy::Error::Serialization("std::vector<std::string>[]: unexpected type on the wire");
    parsed_item = item->as_string();
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeStringSet(const std::vector<std::string>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(smithy::Document(item));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<std::string>> DeserializeStringSet(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<std::string>: expected a list on the wire");
  std::vector<std::string> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<std::string>: null element in a dense list");
    std::string parsed_item{};
    if (!item->is_string()) return smithy::Error::Serialization("std::vector<std::string>[]: unexpected type on the wire");
    parsed_item = item->as_string();
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeFooEnumSet(const std::vector<FooEnum>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(smithy::Document(std::string(item.ToString())));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<FooEnum>> DeserializeFooEnumSet(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<FooEnum>: expected a list on the wire");
  std::vector<FooEnum> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<FooEnum>: null element in a dense list");
    FooEnum parsed_item{};
    if (!item->is_string()) return smithy::Error::Serialization("std::vector<FooEnum>[]: unexpected type on the wire");
    parsed_item = FooEnum::FromString(item->as_string());
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeIntegerEnumSet(const std::vector<IntegerEnum>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(smithy::Document(static_cast<std::int64_t>(item)));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<IntegerEnum>> DeserializeIntegerEnumSet(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<IntegerEnum>: expected a list on the wire");
  std::vector<IntegerEnum> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<IntegerEnum>: null element in a dense list");
    IntegerEnum parsed_item{};
    if (!item->is_int()) return smithy::Error::Serialization("std::vector<IntegerEnum>[]: unexpected type on the wire");
    parsed_item = static_cast<IntegerEnum>(item->as_int());
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeIntegerSet(const std::vector<std::int32_t>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(smithy::Document(static_cast<std::int64_t>(item)));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<std::int32_t>> DeserializeIntegerSet(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<std::int32_t>: expected a list on the wire");
  std::vector<std::int32_t> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<std::int32_t>: null element in a dense list");
    std::int32_t parsed_item{};
    if (!item->is_int()) return smithy::Error::Serialization("std::vector<std::int32_t>[]: unexpected type on the wire");
    if (item->as_int() < -2147483648LL || item->as_int() > 2147483647LL) return smithy::Error::Serialization("std::vector<std::int32_t>[]: value out of range");
    parsed_item = static_cast<std::int32_t>(item->as_int());
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeEnumList(const std::vector<EnumString>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(smithy::Document(std::string(item.ToString())));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<EnumString>> DeserializeEnumList(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<EnumString>: expected a list on the wire");
  std::vector<EnumString> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<EnumString>: null element in a dense list");
    EnumString parsed_item{};
    if (!item->is_string()) return smithy::Error::Serialization("std::vector<EnumString>[]: unexpected type on the wire");
    parsed_item = EnumString::FromString(item->as_string());
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeEnumMap(const std::map<std::string, EnumString>& value) {
  smithy::DocumentMap map;
  for (const auto& [key, item] : value) {
    map.emplace(key, smithy::Document(std::string(item.ToString())));
  }
  return smithy::Document(std::move(map));
}

smithy::Outcome<std::map<std::string, EnumString>> DeserializeEnumMap(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("std::map<std::string, EnumString>: expected a map on the wire");
  std::map<std::string, EnumString> out;
  for (const auto& [key, item_doc] : doc.as_map()) {
    const smithy::Document* item = &item_doc;
    // Tolerant read: null values in dense maps are skipped, not errors.
    if (item->is_null()) continue;
    EnumString parsed_item{};
    if (!item->is_string()) return smithy::Error::Serialization("std::map<std::string, EnumString>{}: unexpected type on the wire");
    parsed_item = EnumString::FromString(item->as_string());
    out.emplace(key, std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeEnumUnion(const EnumUnion& value) {
  smithy::DocumentMap map;
  if (value.is_first()) {
    map.emplace("first", smithy::Document(std::string(value.as_first().ToString())));
  }
  if (value.is_second()) {
    map.emplace("second", smithy::Document(std::string(value.as_second().ToString())));
  }
  return smithy::Document(std::move(map));
}

smithy::Outcome<EnumUnion> DeserializeEnumUnion(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("EnumUnion: expected a map on the wire");
  if (doc.as_map().size() - (doc.Find("__type") != nullptr ? 1 : 0) != 1) return smithy::Error::Serialization("EnumUnion: expected exactly one union member");
  if (const smithy::Document* member = doc.Find("first"); member != nullptr && !member->is_null()) {
    EnumString parsed_member{};
    if (!member->is_string()) return smithy::Error::Serialization("EnumUnion.first: unexpected type on the wire");
    parsed_member = EnumString::FromString(member->as_string());
    return EnumUnion::FromFirst(std::move(parsed_member));
  }
  if (const smithy::Document* member = doc.Find("second"); member != nullptr && !member->is_null()) {
    EnumString parsed_member{};
    if (!member->is_string()) return smithy::Error::Serialization("EnumUnion.second: unexpected type on the wire");
    parsed_member = EnumString::FromString(member->as_string());
    return EnumUnion::FromSecond(std::move(parsed_member));
  }
  return smithy::Error::Serialization("EnumUnion: unknown or missing union member");
}

smithy::Document SerializeLengthList(const std::vector<std::string>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(smithy::Document(item));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<std::string>> DeserializeLengthList(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<std::string>: expected a list on the wire");
  std::vector<std::string> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<std::string>: null element in a dense list");
    std::string parsed_item{};
    if (!item->is_string()) return smithy::Error::Serialization("std::vector<std::string>[]: unexpected type on the wire");
    parsed_item = item->as_string();
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeLengthMap(const std::map<std::string, std::vector<std::string>>& value) {
  smithy::DocumentMap map;
  for (const auto& [key, item] : value) {
    map.emplace(key, SerializeLengthList(item));
  }
  return smithy::Document(std::move(map));
}

smithy::Outcome<std::map<std::string, std::vector<std::string>>> DeserializeLengthMap(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("std::map<std::string, std::vector<std::string>>: expected a map on the wire");
  std::map<std::string, std::vector<std::string>> out;
  for (const auto& [key, item_doc] : doc.as_map()) {
    const smithy::Document* item = &item_doc;
    // Tolerant read: null values in dense maps are skipped, not errors.
    if (item->is_null()) continue;
    std::vector<std::string> parsed_item{};
    {
      auto parsed = DeserializeLengthList(*item);
      if (!parsed) return std::move(parsed).error();
      parsed_item = std::move(*parsed);
    }
    out.emplace(key, std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeMalformedEnumInput(const MalformedEnumInput& value) {
  smithy::DocumentMap map;
  if (value.string.has_value()) {
    map.emplace("string", smithy::Document(std::string((*value.string).ToString())));
  }
  if (value.stringWithEnumTrait.has_value()) {
    map.emplace("stringWithEnumTrait", smithy::Document(std::string((*value.stringWithEnumTrait).ToString())));
  }
  if (value.list.has_value()) {
    map.emplace("list", SerializeEnumList((*value.list)));
  }
  if (value.map.has_value()) {
    map.emplace("map", SerializeEnumMap((*value.map)));
  }
  if (value.union_.has_value()) {
    map.emplace("union", SerializeEnumUnion((*value.union_)));
  }
  return smithy::Document(std::move(map));
}

smithy::Outcome<MalformedEnumInput> DeserializeMalformedEnumInput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MalformedEnumInput: expected a map on the wire");
  MalformedEnumInput out;
  {
    const smithy::Document* member = doc.Find("string");
    if (member != nullptr && !member->is_null()) {
      EnumString parsed_member{};
      if (!member->is_string()) return smithy::Error::Serialization("MalformedEnumInput.string: unexpected type on the wire");
      parsed_member = EnumString::FromString(member->as_string());
      out.string = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("stringWithEnumTrait");
    if (member != nullptr && !member->is_null()) {
      EnumTraitString parsed_member{};
      if (!member->is_string()) return smithy::Error::Serialization("MalformedEnumInput.stringWithEnumTrait: unexpected type on the wire");
      parsed_member = EnumTraitString::FromString(member->as_string());
      out.stringWithEnumTrait = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("list");
    if (member != nullptr && !member->is_null()) {
      std::vector<EnumString> parsed_member{};
      {
        auto parsed = DeserializeEnumList(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.list = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("map");
    if (member != nullptr && !member->is_null()) {
      std::map<std::string, EnumString> parsed_member{};
      {
        auto parsed = DeserializeEnumMap(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.map = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("union");
    if (member != nullptr && !member->is_null()) {
      EnumUnion parsed_member{};
      {
        auto parsed = DeserializeEnumUnion(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.union_ = std::move(parsed_member);
    }
  }
  return out;
}

smithy::Document SerializeMalformedEnumOutput(const MalformedEnumOutput& value) {
  smithy::DocumentMap map;
  return smithy::Document(std::move(map));
}

smithy::Outcome<MalformedEnumOutput> DeserializeMalformedEnumOutput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MalformedEnumOutput: expected a map on the wire");
  MalformedEnumOutput out;
  return out;
}

smithy::Document SerializeValidationExceptionField(const ValidationExceptionField& value) {
  smithy::DocumentMap map;
  map.emplace("path", smithy::Document(value.path));
  map.emplace("message", smithy::Document(value.message));
  return smithy::Document(std::move(map));
}

smithy::Outcome<ValidationExceptionField> DeserializeValidationExceptionField(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("ValidationExceptionField: expected a map on the wire");
  ValidationExceptionField out;
  {
    const smithy::Document* member = doc.Find("path");
    if (member == nullptr || member->is_null()) {
      return smithy::Error::Serialization("ValidationExceptionField: missing required member: path");
    }
    if (!member->is_string()) return smithy::Error::Serialization("ValidationExceptionField.path: unexpected type on the wire");
    out.path = member->as_string();
  }
  {
    const smithy::Document* member = doc.Find("message");
    if (member == nullptr || member->is_null()) {
      return smithy::Error::Serialization("ValidationExceptionField: missing required member: message");
    }
    if (!member->is_string()) return smithy::Error::Serialization("ValidationExceptionField.message: unexpected type on the wire");
    out.message = member->as_string();
  }
  return out;
}

smithy::Document SerializeValidationExceptionFieldList(const std::vector<ValidationExceptionField>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(SerializeValidationExceptionField(item));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<ValidationExceptionField>> DeserializeValidationExceptionFieldList(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<ValidationExceptionField>: expected a list on the wire");
  std::vector<ValidationExceptionField> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<ValidationExceptionField>: null element in a dense list");
    ValidationExceptionField parsed_item{};
    {
      auto parsed = DeserializeValidationExceptionField(*item);
      if (!parsed) return std::move(parsed).error();
      parsed_item = std::move(*parsed);
    }
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeValidationException(const ValidationException& value) {
  smithy::DocumentMap map;
  map.emplace("message", smithy::Document(value.message));
  if (value.fieldList.has_value()) {
    map.emplace("fieldList", SerializeValidationExceptionFieldList((*value.fieldList)));
  }
  return smithy::Document(std::move(map));
}

smithy::Outcome<ValidationException> DeserializeValidationException(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("ValidationException: expected a map on the wire");
  ValidationException out;
  {
    const smithy::Document* member = doc.Find("message");
    if (member == nullptr || member->is_null()) {
      return smithy::Error::Serialization("ValidationException: missing required member: message");
    }
    if (!member->is_string()) return smithy::Error::Serialization("ValidationException.message: unexpected type on the wire");
    out.message = member->as_string();
  }
  {
    const smithy::Document* member = doc.Find("fieldList");
    if (member != nullptr && !member->is_null()) {
      std::vector<ValidationExceptionField> parsed_member{};
      {
        auto parsed = DeserializeValidationExceptionFieldList(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.fieldList = std::move(parsed_member);
    }
  }
  return out;
}

smithy::Document SerializeMalformedLengthInput(const MalformedLengthInput& value) {
  smithy::DocumentMap map;
  if (value.blob.has_value()) {
    map.emplace("blob", smithy::Document((*value.blob)));
  }
  if (value.string.has_value()) {
    map.emplace("string", smithy::Document((*value.string)));
  }
  if (value.minString.has_value()) {
    map.emplace("minString", smithy::Document((*value.minString)));
  }
  if (value.maxString.has_value()) {
    map.emplace("maxString", smithy::Document((*value.maxString)));
  }
  if (value.list.has_value()) {
    map.emplace("list", SerializeLengthList((*value.list)));
  }
  if (value.map.has_value()) {
    map.emplace("map", SerializeLengthMap((*value.map)));
  }
  return smithy::Document(std::move(map));
}

smithy::Outcome<MalformedLengthInput> DeserializeMalformedLengthInput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MalformedLengthInput: expected a map on the wire");
  MalformedLengthInput out;
  {
    const smithy::Document* member = doc.Find("blob");
    if (member != nullptr && !member->is_null()) {
      smithy::Blob parsed_member{};
      {
        auto parsed = smithy::BlobFromDocument(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.blob = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("string");
    if (member != nullptr && !member->is_null()) {
      std::string parsed_member{};
      if (!member->is_string()) return smithy::Error::Serialization("MalformedLengthInput.string: unexpected type on the wire");
      parsed_member = member->as_string();
      out.string = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("minString");
    if (member != nullptr && !member->is_null()) {
      std::string parsed_member{};
      if (!member->is_string()) return smithy::Error::Serialization("MalformedLengthInput.minString: unexpected type on the wire");
      parsed_member = member->as_string();
      out.minString = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("maxString");
    if (member != nullptr && !member->is_null()) {
      std::string parsed_member{};
      if (!member->is_string()) return smithy::Error::Serialization("MalformedLengthInput.maxString: unexpected type on the wire");
      parsed_member = member->as_string();
      out.maxString = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("list");
    if (member != nullptr && !member->is_null()) {
      std::vector<std::string> parsed_member{};
      {
        auto parsed = DeserializeLengthList(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.list = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("map");
    if (member != nullptr && !member->is_null()) {
      std::map<std::string, std::vector<std::string>> parsed_member{};
      {
        auto parsed = DeserializeLengthMap(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.map = std::move(parsed_member);
    }
  }
  return out;
}

smithy::Document SerializeMalformedLengthOutput(const MalformedLengthOutput& value) {
  smithy::DocumentMap map;
  return smithy::Document(std::move(map));
}

smithy::Outcome<MalformedLengthOutput> DeserializeMalformedLengthOutput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MalformedLengthOutput: expected a map on the wire");
  MalformedLengthOutput out;
  return out;
}

smithy::Document SerializeMalformedLengthOverrideInput(const MalformedLengthOverrideInput& value) {
  smithy::DocumentMap map;
  if (value.blob.has_value()) {
    map.emplace("blob", smithy::Document((*value.blob)));
  }
  if (value.string.has_value()) {
    map.emplace("string", smithy::Document((*value.string)));
  }
  if (value.minString.has_value()) {
    map.emplace("minString", smithy::Document((*value.minString)));
  }
  if (value.maxString.has_value()) {
    map.emplace("maxString", smithy::Document((*value.maxString)));
  }
  if (value.list.has_value()) {
    map.emplace("list", SerializeLengthList((*value.list)));
  }
  if (value.map.has_value()) {
    map.emplace("map", SerializeLengthMap((*value.map)));
  }
  return smithy::Document(std::move(map));
}

smithy::Outcome<MalformedLengthOverrideInput> DeserializeMalformedLengthOverrideInput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MalformedLengthOverrideInput: expected a map on the wire");
  MalformedLengthOverrideInput out;
  {
    const smithy::Document* member = doc.Find("blob");
    if (member != nullptr && !member->is_null()) {
      smithy::Blob parsed_member{};
      {
        auto parsed = smithy::BlobFromDocument(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.blob = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("string");
    if (member != nullptr && !member->is_null()) {
      std::string parsed_member{};
      if (!member->is_string()) return smithy::Error::Serialization("MalformedLengthOverrideInput.string: unexpected type on the wire");
      parsed_member = member->as_string();
      out.string = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("minString");
    if (member != nullptr && !member->is_null()) {
      std::string parsed_member{};
      if (!member->is_string()) return smithy::Error::Serialization("MalformedLengthOverrideInput.minString: unexpected type on the wire");
      parsed_member = member->as_string();
      out.minString = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("maxString");
    if (member != nullptr && !member->is_null()) {
      std::string parsed_member{};
      if (!member->is_string()) return smithy::Error::Serialization("MalformedLengthOverrideInput.maxString: unexpected type on the wire");
      parsed_member = member->as_string();
      out.maxString = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("list");
    if (member != nullptr && !member->is_null()) {
      std::vector<std::string> parsed_member{};
      {
        auto parsed = DeserializeLengthList(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.list = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("map");
    if (member != nullptr && !member->is_null()) {
      std::map<std::string, std::vector<std::string>> parsed_member{};
      {
        auto parsed = DeserializeLengthMap(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.map = std::move(parsed_member);
    }
  }
  return out;
}

smithy::Document SerializeMalformedLengthOverrideOutput(const MalformedLengthOverrideOutput& value) {
  smithy::DocumentMap map;
  return smithy::Document(std::move(map));
}

smithy::Outcome<MalformedLengthOverrideOutput> DeserializeMalformedLengthOverrideOutput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MalformedLengthOverrideOutput: expected a map on the wire");
  MalformedLengthOverrideOutput out;
  return out;
}

smithy::Document SerializeMalformedLengthQueryStringInput(const MalformedLengthQueryStringInput& value) {
  smithy::DocumentMap map;
  if (value.string.has_value()) {
    map.emplace("string", smithy::Document((*value.string)));
  }
  return smithy::Document(std::move(map));
}

smithy::Outcome<MalformedLengthQueryStringInput> DeserializeMalformedLengthQueryStringInput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MalformedLengthQueryStringInput: expected a map on the wire");
  MalformedLengthQueryStringInput out;
  {
    const smithy::Document* member = doc.Find("string");
    if (member != nullptr && !member->is_null()) {
      std::string parsed_member{};
      if (!member->is_string()) return smithy::Error::Serialization("MalformedLengthQueryStringInput.string: unexpected type on the wire");
      parsed_member = member->as_string();
      out.string = std::move(parsed_member);
    }
  }
  return out;
}

smithy::Document SerializeMalformedLengthQueryStringOutput(const MalformedLengthQueryStringOutput& value) {
  smithy::DocumentMap map;
  return smithy::Document(std::move(map));
}

smithy::Outcome<MalformedLengthQueryStringOutput> DeserializeMalformedLengthQueryStringOutput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MalformedLengthQueryStringOutput: expected a map on the wire");
  MalformedLengthQueryStringOutput out;
  return out;
}

smithy::Document SerializePatternList(const std::vector<std::string>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(smithy::Document(item));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<std::string>> DeserializePatternList(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<std::string>: expected a list on the wire");
  std::vector<std::string> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<std::string>: null element in a dense list");
    std::string parsed_item{};
    if (!item->is_string()) return smithy::Error::Serialization("std::vector<std::string>[]: unexpected type on the wire");
    parsed_item = item->as_string();
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializePatternMap(const std::map<std::string, std::string>& value) {
  smithy::DocumentMap map;
  for (const auto& [key, item] : value) {
    map.emplace(key, smithy::Document(item));
  }
  return smithy::Document(std::move(map));
}

smithy::Outcome<std::map<std::string, std::string>> DeserializePatternMap(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("std::map<std::string, std::string>: expected a map on the wire");
  std::map<std::string, std::string> out;
  for (const auto& [key, item_doc] : doc.as_map()) {
    const smithy::Document* item = &item_doc;
    // Tolerant read: null values in dense maps are skipped, not errors.
    if (item->is_null()) continue;
    std::string parsed_item{};
    if (!item->is_string()) return smithy::Error::Serialization("std::map<std::string, std::string>{}: unexpected type on the wire");
    parsed_item = item->as_string();
    out.emplace(key, std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializePatternUnion(const PatternUnion& value) {
  smithy::DocumentMap map;
  if (value.is_first()) {
    map.emplace("first", smithy::Document(value.as_first()));
  }
  if (value.is_second()) {
    map.emplace("second", smithy::Document(value.as_second()));
  }
  return smithy::Document(std::move(map));
}

smithy::Outcome<PatternUnion> DeserializePatternUnion(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("PatternUnion: expected a map on the wire");
  if (doc.as_map().size() - (doc.Find("__type") != nullptr ? 1 : 0) != 1) return smithy::Error::Serialization("PatternUnion: expected exactly one union member");
  if (const smithy::Document* member = doc.Find("first"); member != nullptr && !member->is_null()) {
    std::string parsed_member{};
    if (!member->is_string()) return smithy::Error::Serialization("PatternUnion.first: unexpected type on the wire");
    parsed_member = member->as_string();
    return PatternUnion::FromFirst(std::move(parsed_member));
  }
  if (const smithy::Document* member = doc.Find("second"); member != nullptr && !member->is_null()) {
    std::string parsed_member{};
    if (!member->is_string()) return smithy::Error::Serialization("PatternUnion.second: unexpected type on the wire");
    parsed_member = member->as_string();
    return PatternUnion::FromSecond(std::move(parsed_member));
  }
  return smithy::Error::Serialization("PatternUnion: unknown or missing union member");
}

smithy::Document SerializeMalformedPatternInput(const MalformedPatternInput& value) {
  smithy::DocumentMap map;
  if (value.string.has_value()) {
    map.emplace("string", smithy::Document((*value.string)));
  }
  if (value.evilString.has_value()) {
    map.emplace("evilString", smithy::Document((*value.evilString)));
  }
  if (value.list.has_value()) {
    map.emplace("list", SerializePatternList((*value.list)));
  }
  if (value.map.has_value()) {
    map.emplace("map", SerializePatternMap((*value.map)));
  }
  if (value.union_.has_value()) {
    map.emplace("union", SerializePatternUnion((*value.union_)));
  }
  return smithy::Document(std::move(map));
}

smithy::Outcome<MalformedPatternInput> DeserializeMalformedPatternInput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MalformedPatternInput: expected a map on the wire");
  MalformedPatternInput out;
  {
    const smithy::Document* member = doc.Find("string");
    if (member != nullptr && !member->is_null()) {
      std::string parsed_member{};
      if (!member->is_string()) return smithy::Error::Serialization("MalformedPatternInput.string: unexpected type on the wire");
      parsed_member = member->as_string();
      out.string = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("evilString");
    if (member != nullptr && !member->is_null()) {
      std::string parsed_member{};
      if (!member->is_string()) return smithy::Error::Serialization("MalformedPatternInput.evilString: unexpected type on the wire");
      parsed_member = member->as_string();
      out.evilString = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("list");
    if (member != nullptr && !member->is_null()) {
      std::vector<std::string> parsed_member{};
      {
        auto parsed = DeserializePatternList(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.list = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("map");
    if (member != nullptr && !member->is_null()) {
      std::map<std::string, std::string> parsed_member{};
      {
        auto parsed = DeserializePatternMap(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.map = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("union");
    if (member != nullptr && !member->is_null()) {
      PatternUnion parsed_member{};
      {
        auto parsed = DeserializePatternUnion(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.union_ = std::move(parsed_member);
    }
  }
  return out;
}

smithy::Document SerializeMalformedPatternOutput(const MalformedPatternOutput& value) {
  smithy::DocumentMap map;
  return smithy::Document(std::move(map));
}

smithy::Outcome<MalformedPatternOutput> DeserializeMalformedPatternOutput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MalformedPatternOutput: expected a map on the wire");
  MalformedPatternOutput out;
  return out;
}

smithy::Document SerializePatternListOverride(const std::vector<std::string>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(smithy::Document(item));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<std::string>> DeserializePatternListOverride(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<std::string>: expected a list on the wire");
  std::vector<std::string> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<std::string>: null element in a dense list");
    std::string parsed_item{};
    if (!item->is_string()) return smithy::Error::Serialization("std::vector<std::string>[]: unexpected type on the wire");
    parsed_item = item->as_string();
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializePatternMapOverride(const std::map<std::string, std::string>& value) {
  smithy::DocumentMap map;
  for (const auto& [key, item] : value) {
    map.emplace(key, smithy::Document(item));
  }
  return smithy::Document(std::move(map));
}

smithy::Outcome<std::map<std::string, std::string>> DeserializePatternMapOverride(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("std::map<std::string, std::string>: expected a map on the wire");
  std::map<std::string, std::string> out;
  for (const auto& [key, item_doc] : doc.as_map()) {
    const smithy::Document* item = &item_doc;
    // Tolerant read: null values in dense maps are skipped, not errors.
    if (item->is_null()) continue;
    std::string parsed_item{};
    if (!item->is_string()) return smithy::Error::Serialization("std::map<std::string, std::string>{}: unexpected type on the wire");
    parsed_item = item->as_string();
    out.emplace(key, std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializePatternUnionOverride(const PatternUnionOverride& value) {
  smithy::DocumentMap map;
  if (value.is_first()) {
    map.emplace("first", smithy::Document(value.as_first()));
  }
  if (value.is_second()) {
    map.emplace("second", smithy::Document(value.as_second()));
  }
  return smithy::Document(std::move(map));
}

smithy::Outcome<PatternUnionOverride> DeserializePatternUnionOverride(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("PatternUnionOverride: expected a map on the wire");
  if (doc.as_map().size() - (doc.Find("__type") != nullptr ? 1 : 0) != 1) return smithy::Error::Serialization("PatternUnionOverride: expected exactly one union member");
  if (const smithy::Document* member = doc.Find("first"); member != nullptr && !member->is_null()) {
    std::string parsed_member{};
    if (!member->is_string()) return smithy::Error::Serialization("PatternUnionOverride.first: unexpected type on the wire");
    parsed_member = member->as_string();
    return PatternUnionOverride::FromFirst(std::move(parsed_member));
  }
  if (const smithy::Document* member = doc.Find("second"); member != nullptr && !member->is_null()) {
    std::string parsed_member{};
    if (!member->is_string()) return smithy::Error::Serialization("PatternUnionOverride.second: unexpected type on the wire");
    parsed_member = member->as_string();
    return PatternUnionOverride::FromSecond(std::move(parsed_member));
  }
  return smithy::Error::Serialization("PatternUnionOverride: unknown or missing union member");
}

smithy::Document SerializeMalformedPatternOverrideInput(const MalformedPatternOverrideInput& value) {
  smithy::DocumentMap map;
  if (value.string.has_value()) {
    map.emplace("string", smithy::Document((*value.string)));
  }
  if (value.list.has_value()) {
    map.emplace("list", SerializePatternListOverride((*value.list)));
  }
  if (value.map.has_value()) {
    map.emplace("map", SerializePatternMapOverride((*value.map)));
  }
  if (value.union_.has_value()) {
    map.emplace("union", SerializePatternUnionOverride((*value.union_)));
  }
  return smithy::Document(std::move(map));
}

smithy::Outcome<MalformedPatternOverrideInput> DeserializeMalformedPatternOverrideInput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MalformedPatternOverrideInput: expected a map on the wire");
  MalformedPatternOverrideInput out;
  {
    const smithy::Document* member = doc.Find("string");
    if (member != nullptr && !member->is_null()) {
      std::string parsed_member{};
      if (!member->is_string()) return smithy::Error::Serialization("MalformedPatternOverrideInput.string: unexpected type on the wire");
      parsed_member = member->as_string();
      out.string = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("list");
    if (member != nullptr && !member->is_null()) {
      std::vector<std::string> parsed_member{};
      {
        auto parsed = DeserializePatternListOverride(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.list = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("map");
    if (member != nullptr && !member->is_null()) {
      std::map<std::string, std::string> parsed_member{};
      {
        auto parsed = DeserializePatternMapOverride(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.map = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("union");
    if (member != nullptr && !member->is_null()) {
      PatternUnionOverride parsed_member{};
      {
        auto parsed = DeserializePatternUnionOverride(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.union_ = std::move(parsed_member);
    }
  }
  return out;
}

smithy::Document SerializeMalformedPatternOverrideOutput(const MalformedPatternOverrideOutput& value) {
  smithy::DocumentMap map;
  return smithy::Document(std::move(map));
}

smithy::Outcome<MalformedPatternOverrideOutput> DeserializeMalformedPatternOverrideOutput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MalformedPatternOverrideOutput: expected a map on the wire");
  MalformedPatternOverrideOutput out;
  return out;
}

smithy::Document SerializeMalformedRangeInput(const MalformedRangeInput& value) {
  smithy::DocumentMap map;
  if (value.byte.has_value()) {
    map.emplace("byte", smithy::Document(static_cast<std::int64_t>((*value.byte))));
  }
  if (value.minByte.has_value()) {
    map.emplace("minByte", smithy::Document(static_cast<std::int64_t>((*value.minByte))));
  }
  if (value.maxByte.has_value()) {
    map.emplace("maxByte", smithy::Document(static_cast<std::int64_t>((*value.maxByte))));
  }
  if (value.short_.has_value()) {
    map.emplace("short", smithy::Document(static_cast<std::int64_t>((*value.short_))));
  }
  if (value.minShort.has_value()) {
    map.emplace("minShort", smithy::Document(static_cast<std::int64_t>((*value.minShort))));
  }
  if (value.maxShort.has_value()) {
    map.emplace("maxShort", smithy::Document(static_cast<std::int64_t>((*value.maxShort))));
  }
  if (value.integer.has_value()) {
    map.emplace("integer", smithy::Document(static_cast<std::int64_t>((*value.integer))));
  }
  if (value.minInteger.has_value()) {
    map.emplace("minInteger", smithy::Document(static_cast<std::int64_t>((*value.minInteger))));
  }
  if (value.maxInteger.has_value()) {
    map.emplace("maxInteger", smithy::Document(static_cast<std::int64_t>((*value.maxInteger))));
  }
  if (value.long_.has_value()) {
    map.emplace("long", smithy::Document(static_cast<std::int64_t>((*value.long_))));
  }
  if (value.minLong.has_value()) {
    map.emplace("minLong", smithy::Document(static_cast<std::int64_t>((*value.minLong))));
  }
  if (value.maxLong.has_value()) {
    map.emplace("maxLong", smithy::Document(static_cast<std::int64_t>((*value.maxLong))));
  }
  if (value.float_.has_value()) {
    map.emplace("float", smithy::Document(static_cast<double>((*value.float_))));
  }
  if (value.minFloat.has_value()) {
    map.emplace("minFloat", smithy::Document(static_cast<double>((*value.minFloat))));
  }
  if (value.maxFloat.has_value()) {
    map.emplace("maxFloat", smithy::Document(static_cast<double>((*value.maxFloat))));
  }
  return smithy::Document(std::move(map));
}

smithy::Outcome<MalformedRangeInput> DeserializeMalformedRangeInput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MalformedRangeInput: expected a map on the wire");
  MalformedRangeInput out;
  {
    const smithy::Document* member = doc.Find("byte");
    if (member != nullptr && !member->is_null()) {
      std::int8_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeInput.byte: unexpected type on the wire");
      if (member->as_int() < -128 || member->as_int() > 127) return smithy::Error::Serialization("MalformedRangeInput.byte: value out of range");
      parsed_member = static_cast<std::int8_t>(member->as_int());
      out.byte = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("minByte");
    if (member != nullptr && !member->is_null()) {
      std::int8_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeInput.minByte: unexpected type on the wire");
      if (member->as_int() < -128 || member->as_int() > 127) return smithy::Error::Serialization("MalformedRangeInput.minByte: value out of range");
      parsed_member = static_cast<std::int8_t>(member->as_int());
      out.minByte = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("maxByte");
    if (member != nullptr && !member->is_null()) {
      std::int8_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeInput.maxByte: unexpected type on the wire");
      if (member->as_int() < -128 || member->as_int() > 127) return smithy::Error::Serialization("MalformedRangeInput.maxByte: value out of range");
      parsed_member = static_cast<std::int8_t>(member->as_int());
      out.maxByte = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("short");
    if (member != nullptr && !member->is_null()) {
      std::int16_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeInput.short: unexpected type on the wire");
      if (member->as_int() < -32768 || member->as_int() > 32767) return smithy::Error::Serialization("MalformedRangeInput.short: value out of range");
      parsed_member = static_cast<std::int16_t>(member->as_int());
      out.short_ = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("minShort");
    if (member != nullptr && !member->is_null()) {
      std::int16_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeInput.minShort: unexpected type on the wire");
      if (member->as_int() < -32768 || member->as_int() > 32767) return smithy::Error::Serialization("MalformedRangeInput.minShort: value out of range");
      parsed_member = static_cast<std::int16_t>(member->as_int());
      out.minShort = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("maxShort");
    if (member != nullptr && !member->is_null()) {
      std::int16_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeInput.maxShort: unexpected type on the wire");
      if (member->as_int() < -32768 || member->as_int() > 32767) return smithy::Error::Serialization("MalformedRangeInput.maxShort: value out of range");
      parsed_member = static_cast<std::int16_t>(member->as_int());
      out.maxShort = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("integer");
    if (member != nullptr && !member->is_null()) {
      std::int32_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeInput.integer: unexpected type on the wire");
      if (member->as_int() < -2147483648LL || member->as_int() > 2147483647LL) return smithy::Error::Serialization("MalformedRangeInput.integer: value out of range");
      parsed_member = static_cast<std::int32_t>(member->as_int());
      out.integer = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("minInteger");
    if (member != nullptr && !member->is_null()) {
      std::int32_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeInput.minInteger: unexpected type on the wire");
      if (member->as_int() < -2147483648LL || member->as_int() > 2147483647LL) return smithy::Error::Serialization("MalformedRangeInput.minInteger: value out of range");
      parsed_member = static_cast<std::int32_t>(member->as_int());
      out.minInteger = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("maxInteger");
    if (member != nullptr && !member->is_null()) {
      std::int32_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeInput.maxInteger: unexpected type on the wire");
      if (member->as_int() < -2147483648LL || member->as_int() > 2147483647LL) return smithy::Error::Serialization("MalformedRangeInput.maxInteger: value out of range");
      parsed_member = static_cast<std::int32_t>(member->as_int());
      out.maxInteger = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("long");
    if (member != nullptr && !member->is_null()) {
      std::int64_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeInput.long: unexpected type on the wire");
      parsed_member = static_cast<std::int64_t>(member->as_int());
      out.long_ = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("minLong");
    if (member != nullptr && !member->is_null()) {
      std::int64_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeInput.minLong: unexpected type on the wire");
      parsed_member = static_cast<std::int64_t>(member->as_int());
      out.minLong = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("maxLong");
    if (member != nullptr && !member->is_null()) {
      std::int64_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeInput.maxLong: unexpected type on the wire");
      parsed_member = static_cast<std::int64_t>(member->as_int());
      out.maxLong = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("float");
    if (member != nullptr && !member->is_null()) {
      float parsed_member{};
      {
        auto parsed = smithy::DoubleFromDocument(*member);
        if (!parsed) return smithy::Error::Serialization("MalformedRangeInput.float: expected a number");
        parsed_member = static_cast<float>(*parsed);
      }
      out.float_ = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("minFloat");
    if (member != nullptr && !member->is_null()) {
      float parsed_member{};
      {
        auto parsed = smithy::DoubleFromDocument(*member);
        if (!parsed) return smithy::Error::Serialization("MalformedRangeInput.minFloat: expected a number");
        parsed_member = static_cast<float>(*parsed);
      }
      out.minFloat = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("maxFloat");
    if (member != nullptr && !member->is_null()) {
      float parsed_member{};
      {
        auto parsed = smithy::DoubleFromDocument(*member);
        if (!parsed) return smithy::Error::Serialization("MalformedRangeInput.maxFloat: expected a number");
        parsed_member = static_cast<float>(*parsed);
      }
      out.maxFloat = std::move(parsed_member);
    }
  }
  return out;
}

smithy::Document SerializeMalformedRangeOutput(const MalformedRangeOutput& value) {
  smithy::DocumentMap map;
  return smithy::Document(std::move(map));
}

smithy::Outcome<MalformedRangeOutput> DeserializeMalformedRangeOutput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MalformedRangeOutput: expected a map on the wire");
  MalformedRangeOutput out;
  return out;
}

smithy::Document SerializeMalformedRangeOverrideInput(const MalformedRangeOverrideInput& value) {
  smithy::DocumentMap map;
  if (value.byte.has_value()) {
    map.emplace("byte", smithy::Document(static_cast<std::int64_t>((*value.byte))));
  }
  if (value.minByte.has_value()) {
    map.emplace("minByte", smithy::Document(static_cast<std::int64_t>((*value.minByte))));
  }
  if (value.maxByte.has_value()) {
    map.emplace("maxByte", smithy::Document(static_cast<std::int64_t>((*value.maxByte))));
  }
  if (value.short_.has_value()) {
    map.emplace("short", smithy::Document(static_cast<std::int64_t>((*value.short_))));
  }
  if (value.minShort.has_value()) {
    map.emplace("minShort", smithy::Document(static_cast<std::int64_t>((*value.minShort))));
  }
  if (value.maxShort.has_value()) {
    map.emplace("maxShort", smithy::Document(static_cast<std::int64_t>((*value.maxShort))));
  }
  if (value.integer.has_value()) {
    map.emplace("integer", smithy::Document(static_cast<std::int64_t>((*value.integer))));
  }
  if (value.minInteger.has_value()) {
    map.emplace("minInteger", smithy::Document(static_cast<std::int64_t>((*value.minInteger))));
  }
  if (value.maxInteger.has_value()) {
    map.emplace("maxInteger", smithy::Document(static_cast<std::int64_t>((*value.maxInteger))));
  }
  if (value.long_.has_value()) {
    map.emplace("long", smithy::Document(static_cast<std::int64_t>((*value.long_))));
  }
  if (value.minLong.has_value()) {
    map.emplace("minLong", smithy::Document(static_cast<std::int64_t>((*value.minLong))));
  }
  if (value.maxLong.has_value()) {
    map.emplace("maxLong", smithy::Document(static_cast<std::int64_t>((*value.maxLong))));
  }
  if (value.float_.has_value()) {
    map.emplace("float", smithy::Document(static_cast<double>((*value.float_))));
  }
  if (value.minFloat.has_value()) {
    map.emplace("minFloat", smithy::Document(static_cast<double>((*value.minFloat))));
  }
  if (value.maxFloat.has_value()) {
    map.emplace("maxFloat", smithy::Document(static_cast<double>((*value.maxFloat))));
  }
  return smithy::Document(std::move(map));
}

smithy::Outcome<MalformedRangeOverrideInput> DeserializeMalformedRangeOverrideInput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MalformedRangeOverrideInput: expected a map on the wire");
  MalformedRangeOverrideInput out;
  {
    const smithy::Document* member = doc.Find("byte");
    if (member != nullptr && !member->is_null()) {
      std::int8_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeOverrideInput.byte: unexpected type on the wire");
      if (member->as_int() < -128 || member->as_int() > 127) return smithy::Error::Serialization("MalformedRangeOverrideInput.byte: value out of range");
      parsed_member = static_cast<std::int8_t>(member->as_int());
      out.byte = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("minByte");
    if (member != nullptr && !member->is_null()) {
      std::int8_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeOverrideInput.minByte: unexpected type on the wire");
      if (member->as_int() < -128 || member->as_int() > 127) return smithy::Error::Serialization("MalformedRangeOverrideInput.minByte: value out of range");
      parsed_member = static_cast<std::int8_t>(member->as_int());
      out.minByte = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("maxByte");
    if (member != nullptr && !member->is_null()) {
      std::int8_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeOverrideInput.maxByte: unexpected type on the wire");
      if (member->as_int() < -128 || member->as_int() > 127) return smithy::Error::Serialization("MalformedRangeOverrideInput.maxByte: value out of range");
      parsed_member = static_cast<std::int8_t>(member->as_int());
      out.maxByte = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("short");
    if (member != nullptr && !member->is_null()) {
      std::int16_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeOverrideInput.short: unexpected type on the wire");
      if (member->as_int() < -32768 || member->as_int() > 32767) return smithy::Error::Serialization("MalformedRangeOverrideInput.short: value out of range");
      parsed_member = static_cast<std::int16_t>(member->as_int());
      out.short_ = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("minShort");
    if (member != nullptr && !member->is_null()) {
      std::int16_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeOverrideInput.minShort: unexpected type on the wire");
      if (member->as_int() < -32768 || member->as_int() > 32767) return smithy::Error::Serialization("MalformedRangeOverrideInput.minShort: value out of range");
      parsed_member = static_cast<std::int16_t>(member->as_int());
      out.minShort = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("maxShort");
    if (member != nullptr && !member->is_null()) {
      std::int16_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeOverrideInput.maxShort: unexpected type on the wire");
      if (member->as_int() < -32768 || member->as_int() > 32767) return smithy::Error::Serialization("MalformedRangeOverrideInput.maxShort: value out of range");
      parsed_member = static_cast<std::int16_t>(member->as_int());
      out.maxShort = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("integer");
    if (member != nullptr && !member->is_null()) {
      std::int32_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeOverrideInput.integer: unexpected type on the wire");
      if (member->as_int() < -2147483648LL || member->as_int() > 2147483647LL) return smithy::Error::Serialization("MalformedRangeOverrideInput.integer: value out of range");
      parsed_member = static_cast<std::int32_t>(member->as_int());
      out.integer = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("minInteger");
    if (member != nullptr && !member->is_null()) {
      std::int32_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeOverrideInput.minInteger: unexpected type on the wire");
      if (member->as_int() < -2147483648LL || member->as_int() > 2147483647LL) return smithy::Error::Serialization("MalformedRangeOverrideInput.minInteger: value out of range");
      parsed_member = static_cast<std::int32_t>(member->as_int());
      out.minInteger = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("maxInteger");
    if (member != nullptr && !member->is_null()) {
      std::int32_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeOverrideInput.maxInteger: unexpected type on the wire");
      if (member->as_int() < -2147483648LL || member->as_int() > 2147483647LL) return smithy::Error::Serialization("MalformedRangeOverrideInput.maxInteger: value out of range");
      parsed_member = static_cast<std::int32_t>(member->as_int());
      out.maxInteger = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("long");
    if (member != nullptr && !member->is_null()) {
      std::int64_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeOverrideInput.long: unexpected type on the wire");
      parsed_member = static_cast<std::int64_t>(member->as_int());
      out.long_ = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("minLong");
    if (member != nullptr && !member->is_null()) {
      std::int64_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeOverrideInput.minLong: unexpected type on the wire");
      parsed_member = static_cast<std::int64_t>(member->as_int());
      out.minLong = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("maxLong");
    if (member != nullptr && !member->is_null()) {
      std::int64_t parsed_member{};
      if (!member->is_int()) return smithy::Error::Serialization("MalformedRangeOverrideInput.maxLong: unexpected type on the wire");
      parsed_member = static_cast<std::int64_t>(member->as_int());
      out.maxLong = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("float");
    if (member != nullptr && !member->is_null()) {
      float parsed_member{};
      {
        auto parsed = smithy::DoubleFromDocument(*member);
        if (!parsed) return smithy::Error::Serialization("MalformedRangeOverrideInput.float: expected a number");
        parsed_member = static_cast<float>(*parsed);
      }
      out.float_ = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("minFloat");
    if (member != nullptr && !member->is_null()) {
      float parsed_member{};
      {
        auto parsed = smithy::DoubleFromDocument(*member);
        if (!parsed) return smithy::Error::Serialization("MalformedRangeOverrideInput.minFloat: expected a number");
        parsed_member = static_cast<float>(*parsed);
      }
      out.minFloat = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("maxFloat");
    if (member != nullptr && !member->is_null()) {
      float parsed_member{};
      {
        auto parsed = smithy::DoubleFromDocument(*member);
        if (!parsed) return smithy::Error::Serialization("MalformedRangeOverrideInput.maxFloat: expected a number");
        parsed_member = static_cast<float>(*parsed);
      }
      out.maxFloat = std::move(parsed_member);
    }
  }
  return out;
}

smithy::Document SerializeMalformedRangeOverrideOutput(const MalformedRangeOverrideOutput& value) {
  smithy::DocumentMap map;
  return smithy::Document(std::move(map));
}

smithy::Outcome<MalformedRangeOverrideOutput> DeserializeMalformedRangeOverrideOutput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MalformedRangeOverrideOutput: expected a map on the wire");
  MalformedRangeOverrideOutput out;
  return out;
}

smithy::Document SerializeMalformedRequiredInput(const MalformedRequiredInput& value) {
  smithy::DocumentMap map;
  map.emplace("string", smithy::Document(value.string));
  map.emplace("stringInQuery", smithy::Document(value.stringInQuery));
  map.emplace("stringInHeader", smithy::Document(value.stringInHeader));
  return smithy::Document(std::move(map));
}

smithy::Outcome<MalformedRequiredInput> DeserializeMalformedRequiredInput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MalformedRequiredInput: expected a map on the wire");
  MalformedRequiredInput out;
  {
    const smithy::Document* member = doc.Find("string");
    if (member == nullptr || member->is_null()) {
      return smithy::Error::Serialization("MalformedRequiredInput: missing required member: string");
    }
    if (!member->is_string()) return smithy::Error::Serialization("MalformedRequiredInput.string: unexpected type on the wire");
    out.string = member->as_string();
  }
  {
    const smithy::Document* member = doc.Find("stringInQuery");
    if (member == nullptr || member->is_null()) {
      return smithy::Error::Serialization("MalformedRequiredInput: missing required member: stringInQuery");
    }
    if (!member->is_string()) return smithy::Error::Serialization("MalformedRequiredInput.stringInQuery: unexpected type on the wire");
    out.stringInQuery = member->as_string();
  }
  {
    const smithy::Document* member = doc.Find("stringInHeader");
    if (member == nullptr || member->is_null()) {
      return smithy::Error::Serialization("MalformedRequiredInput: missing required member: stringInHeader");
    }
    if (!member->is_string()) return smithy::Error::Serialization("MalformedRequiredInput.stringInHeader: unexpected type on the wire");
    out.stringInHeader = member->as_string();
  }
  return out;
}

smithy::Document SerializeMalformedRequiredOutput(const MalformedRequiredOutput& value) {
  smithy::DocumentMap map;
  return smithy::Document(std::move(map));
}

smithy::Outcome<MalformedRequiredOutput> DeserializeMalformedRequiredOutput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MalformedRequiredOutput: expected a map on the wire");
  MalformedRequiredOutput out;
  return out;
}

smithy::Document SerializeBlobSet(const std::vector<smithy::Blob>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(smithy::Document(item));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<smithy::Blob>> DeserializeBlobSet(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<smithy::Blob>: expected a list on the wire");
  std::vector<smithy::Blob> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<smithy::Blob>: null element in a dense list");
    smithy::Blob parsed_item{};
    {
      auto parsed = smithy::BlobFromDocument(*item);
      if (!parsed) return std::move(parsed).error();
      parsed_item = std::move(*parsed);
    }
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeBooleanSet(const std::vector<bool>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(smithy::Document(item));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<bool>> DeserializeBooleanSet(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<bool>: expected a list on the wire");
  std::vector<bool> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<bool>: null element in a dense list");
    bool parsed_item{};
    if (!item->is_bool()) return smithy::Error::Serialization("std::vector<bool>[]: unexpected type on the wire");
    parsed_item = item->as_bool();
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeByteSet(const std::vector<std::int8_t>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(smithy::Document(static_cast<std::int64_t>(item)));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<std::int8_t>> DeserializeByteSet(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<std::int8_t>: expected a list on the wire");
  std::vector<std::int8_t> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<std::int8_t>: null element in a dense list");
    std::int8_t parsed_item{};
    if (!item->is_int()) return smithy::Error::Serialization("std::vector<std::int8_t>[]: unexpected type on the wire");
    if (item->as_int() < -128 || item->as_int() > 127) return smithy::Error::Serialization("std::vector<std::int8_t>[]: value out of range");
    parsed_item = static_cast<std::int8_t>(item->as_int());
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeDateTimeSet(const std::vector<smithy::Timestamp>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(smithy::Document::FromTimestamp(item, smithy::TimestampFormat::kDateTime));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<smithy::Timestamp>> DeserializeDateTimeSet(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<smithy::Timestamp>: expected a list on the wire");
  std::vector<smithy::Timestamp> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<smithy::Timestamp>: null element in a dense list");
    smithy::Timestamp parsed_item{};
    {
      auto parsed = smithy::TimestampFromDocument(*item, smithy::TimestampFormat::kDateTime);
      if (!parsed) return std::move(parsed).error();
      parsed_item = *parsed;
    }
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeHttpDateSet(const std::vector<smithy::Timestamp>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(smithy::Document::FromTimestamp(item, smithy::TimestampFormat::kHttpDate));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<smithy::Timestamp>> DeserializeHttpDateSet(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<smithy::Timestamp>: expected a list on the wire");
  std::vector<smithy::Timestamp> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<smithy::Timestamp>: null element in a dense list");
    smithy::Timestamp parsed_item{};
    {
      auto parsed = smithy::TimestampFromDocument(*item, smithy::TimestampFormat::kHttpDate);
      if (!parsed) return std::move(parsed).error();
      parsed_item = *parsed;
    }
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeListSet(const std::vector<std::vector<std::string>>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(SerializeStringList(item));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<std::vector<std::string>>> DeserializeListSet(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<std::vector<std::string>>: expected a list on the wire");
  std::vector<std::vector<std::string>> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<std::vector<std::string>>: null element in a dense list");
    std::vector<std::string> parsed_item{};
    {
      auto parsed = DeserializeStringList(*item);
      if (!parsed) return std::move(parsed).error();
      parsed_item = std::move(*parsed);
    }
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeLongSet(const std::vector<std::int64_t>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(smithy::Document(static_cast<std::int64_t>(item)));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<std::int64_t>> DeserializeLongSet(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<std::int64_t>: expected a list on the wire");
  std::vector<std::int64_t> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<std::int64_t>: null element in a dense list");
    std::int64_t parsed_item{};
    if (!item->is_int()) return smithy::Error::Serialization("std::vector<std::int64_t>[]: unexpected type on the wire");
    parsed_item = static_cast<std::int64_t>(item->as_int());
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeShortSet(const std::vector<std::int16_t>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(smithy::Document(static_cast<std::int64_t>(item)));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<std::int16_t>> DeserializeShortSet(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<std::int16_t>: expected a list on the wire");
  std::vector<std::int16_t> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<std::int16_t>: null element in a dense list");
    std::int16_t parsed_item{};
    if (!item->is_int()) return smithy::Error::Serialization("std::vector<std::int16_t>[]: unexpected type on the wire");
    if (item->as_int() < -32768 || item->as_int() > 32767) return smithy::Error::Serialization("std::vector<std::int16_t>[]: value out of range");
    parsed_item = static_cast<std::int16_t>(item->as_int());
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeStructureSet(const std::vector<GreetingStruct>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(SerializeGreetingStruct(item));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<GreetingStruct>> DeserializeStructureSet(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<GreetingStruct>: expected a list on the wire");
  std::vector<GreetingStruct> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<GreetingStruct>: null element in a dense list");
    GreetingStruct parsed_item{};
    {
      auto parsed = DeserializeGreetingStruct(*item);
      if (!parsed) return std::move(parsed).error();
      parsed_item = std::move(*parsed);
    }
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeMissingKeyStructure(const MissingKeyStructure& value) {
  smithy::DocumentMap map;
  map.emplace("hi", smithy::Document(value.hi));
  return smithy::Document(std::move(map));
}

smithy::Outcome<MissingKeyStructure> DeserializeMissingKeyStructure(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MissingKeyStructure: expected a map on the wire");
  MissingKeyStructure out;
  {
    const smithy::Document* member = doc.Find("hi");
    if (member == nullptr || member->is_null()) {
      return smithy::Error::Serialization("MissingKeyStructure: missing required member: hi");
    }
    if (!member->is_string()) return smithy::Error::Serialization("MissingKeyStructure.hi: unexpected type on the wire");
    out.hi = member->as_string();
  }
  return out;
}

smithy::Document SerializeStructureSetWithNoKey(const std::vector<MissingKeyStructure>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(SerializeMissingKeyStructure(item));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<MissingKeyStructure>> DeserializeStructureSetWithNoKey(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<MissingKeyStructure>: expected a list on the wire");
  std::vector<MissingKeyStructure> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<MissingKeyStructure>: null element in a dense list");
    MissingKeyStructure parsed_item{};
    {
      auto parsed = DeserializeMissingKeyStructure(*item);
      if (!parsed) return std::move(parsed).error();
      parsed_item = std::move(*parsed);
    }
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeTimestampSet(const std::vector<smithy::Timestamp>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(smithy::Document::FromTimestamp(item, smithy::TimestampFormat::kEpochSeconds));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<smithy::Timestamp>> DeserializeTimestampSet(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<smithy::Timestamp>: expected a list on the wire");
  std::vector<smithy::Timestamp> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<smithy::Timestamp>: null element in a dense list");
    smithy::Timestamp parsed_item{};
    {
      auto parsed = smithy::TimestampFromDocument(*item, smithy::TimestampFormat::kEpochSeconds);
      if (!parsed) return std::move(parsed).error();
      parsed_item = *parsed;
    }
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeFooUnion(const FooUnion& value) {
  smithy::DocumentMap map;
  if (value.is_string()) {
    map.emplace("string", smithy::Document(value.as_string()));
  }
  if (value.is_integer()) {
    map.emplace("integer", smithy::Document(static_cast<std::int64_t>(value.as_integer())));
  }
  return smithy::Document(std::move(map));
}

smithy::Outcome<FooUnion> DeserializeFooUnion(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("FooUnion: expected a map on the wire");
  if (doc.as_map().size() - (doc.Find("__type") != nullptr ? 1 : 0) != 1) return smithy::Error::Serialization("FooUnion: expected exactly one union member");
  if (const smithy::Document* member = doc.Find("string"); member != nullptr && !member->is_null()) {
    std::string parsed_member{};
    if (!member->is_string()) return smithy::Error::Serialization("FooUnion.string: unexpected type on the wire");
    parsed_member = member->as_string();
    return FooUnion::FromString(std::move(parsed_member));
  }
  if (const smithy::Document* member = doc.Find("integer"); member != nullptr && !member->is_null()) {
    std::int32_t parsed_member{};
    if (!member->is_int()) return smithy::Error::Serialization("FooUnion.integer: unexpected type on the wire");
    if (member->as_int() < -2147483648LL || member->as_int() > 2147483647LL) return smithy::Error::Serialization("FooUnion.integer: value out of range");
    parsed_member = static_cast<std::int32_t>(member->as_int());
    return FooUnion::FromInteger(std::move(parsed_member));
  }
  return smithy::Error::Serialization("FooUnion: unknown or missing union member");
}

smithy::Document SerializeUnionSet(const std::vector<FooUnion>& value) {
  smithy::DocumentList list;
  list.reserve(value.size());
  for (const auto& item : value) {
    list.push_back(SerializeFooUnion(item));
  }
  return smithy::Document(std::move(list));
}

smithy::Outcome<std::vector<FooUnion>> DeserializeUnionSet(const smithy::Document& doc) {
  if (!doc.is_list()) return smithy::Error::Serialization("std::vector<FooUnion>: expected a list on the wire");
  std::vector<FooUnion> out;
  out.reserve(doc.as_list().size());
  for (const smithy::Document& item_doc : doc.as_list()) {
    const smithy::Document* item = &item_doc;
    if (item->is_null()) return smithy::Error::Serialization("std::vector<FooUnion>: null element in a dense list");
    FooUnion parsed_item{};
    {
      auto parsed = DeserializeFooUnion(*item);
      if (!parsed) return std::move(parsed).error();
      parsed_item = std::move(*parsed);
    }
    out.push_back(std::move(parsed_item));
  }
  return out;
}

smithy::Document SerializeMalformedUniqueItemsInput(const MalformedUniqueItemsInput& value) {
  smithy::DocumentMap map;
  if (value.blobList.has_value()) {
    map.emplace("blobList", SerializeBlobSet((*value.blobList)));
  }
  if (value.booleanList.has_value()) {
    map.emplace("booleanList", SerializeBooleanSet((*value.booleanList)));
  }
  if (value.stringList.has_value()) {
    map.emplace("stringList", SerializeStringSet((*value.stringList)));
  }
  if (value.byteList.has_value()) {
    map.emplace("byteList", SerializeByteSet((*value.byteList)));
  }
  if (value.shortList.has_value()) {
    map.emplace("shortList", SerializeShortSet((*value.shortList)));
  }
  if (value.integerList.has_value()) {
    map.emplace("integerList", SerializeIntegerSet((*value.integerList)));
  }
  if (value.longList.has_value()) {
    map.emplace("longList", SerializeLongSet((*value.longList)));
  }
  if (value.timestampList.has_value()) {
    map.emplace("timestampList", SerializeTimestampSet((*value.timestampList)));
  }
  if (value.dateTimeList.has_value()) {
    map.emplace("dateTimeList", SerializeDateTimeSet((*value.dateTimeList)));
  }
  if (value.httpDateList.has_value()) {
    map.emplace("httpDateList", SerializeHttpDateSet((*value.httpDateList)));
  }
  if (value.enumList.has_value()) {
    map.emplace("enumList", SerializeFooEnumSet((*value.enumList)));
  }
  if (value.intEnumList.has_value()) {
    map.emplace("intEnumList", SerializeIntegerEnumSet((*value.intEnumList)));
  }
  if (value.listList.has_value()) {
    map.emplace("listList", SerializeListSet((*value.listList)));
  }
  if (value.structureList.has_value()) {
    map.emplace("structureList", SerializeStructureSet((*value.structureList)));
  }
  if (value.structureListWithNoKey.has_value()) {
    map.emplace("structureListWithNoKey", SerializeStructureSetWithNoKey((*value.structureListWithNoKey)));
  }
  if (value.unionList.has_value()) {
    map.emplace("unionList", SerializeUnionSet((*value.unionList)));
  }
  return smithy::Document(std::move(map));
}

smithy::Outcome<MalformedUniqueItemsInput> DeserializeMalformedUniqueItemsInput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MalformedUniqueItemsInput: expected a map on the wire");
  MalformedUniqueItemsInput out;
  {
    const smithy::Document* member = doc.Find("blobList");
    if (member != nullptr && !member->is_null()) {
      std::vector<smithy::Blob> parsed_member{};
      {
        auto parsed = DeserializeBlobSet(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.blobList = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("booleanList");
    if (member != nullptr && !member->is_null()) {
      std::vector<bool> parsed_member{};
      {
        auto parsed = DeserializeBooleanSet(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.booleanList = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("stringList");
    if (member != nullptr && !member->is_null()) {
      std::vector<std::string> parsed_member{};
      {
        auto parsed = DeserializeStringSet(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.stringList = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("byteList");
    if (member != nullptr && !member->is_null()) {
      std::vector<std::int8_t> parsed_member{};
      {
        auto parsed = DeserializeByteSet(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.byteList = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("shortList");
    if (member != nullptr && !member->is_null()) {
      std::vector<std::int16_t> parsed_member{};
      {
        auto parsed = DeserializeShortSet(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.shortList = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("integerList");
    if (member != nullptr && !member->is_null()) {
      std::vector<std::int32_t> parsed_member{};
      {
        auto parsed = DeserializeIntegerSet(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.integerList = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("longList");
    if (member != nullptr && !member->is_null()) {
      std::vector<std::int64_t> parsed_member{};
      {
        auto parsed = DeserializeLongSet(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.longList = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("timestampList");
    if (member != nullptr && !member->is_null()) {
      std::vector<smithy::Timestamp> parsed_member{};
      {
        auto parsed = DeserializeTimestampSet(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.timestampList = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("dateTimeList");
    if (member != nullptr && !member->is_null()) {
      std::vector<smithy::Timestamp> parsed_member{};
      {
        auto parsed = DeserializeDateTimeSet(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.dateTimeList = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("httpDateList");
    if (member != nullptr && !member->is_null()) {
      std::vector<smithy::Timestamp> parsed_member{};
      {
        auto parsed = DeserializeHttpDateSet(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.httpDateList = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("enumList");
    if (member != nullptr && !member->is_null()) {
      std::vector<FooEnum> parsed_member{};
      {
        auto parsed = DeserializeFooEnumSet(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.enumList = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("intEnumList");
    if (member != nullptr && !member->is_null()) {
      std::vector<IntegerEnum> parsed_member{};
      {
        auto parsed = DeserializeIntegerEnumSet(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.intEnumList = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("listList");
    if (member != nullptr && !member->is_null()) {
      std::vector<std::vector<std::string>> parsed_member{};
      {
        auto parsed = DeserializeListSet(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.listList = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("structureList");
    if (member != nullptr && !member->is_null()) {
      std::vector<GreetingStruct> parsed_member{};
      {
        auto parsed = DeserializeStructureSet(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.structureList = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("structureListWithNoKey");
    if (member != nullptr && !member->is_null()) {
      std::vector<MissingKeyStructure> parsed_member{};
      {
        auto parsed = DeserializeStructureSetWithNoKey(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.structureListWithNoKey = std::move(parsed_member);
    }
  }
  {
    const smithy::Document* member = doc.Find("unionList");
    if (member != nullptr && !member->is_null()) {
      std::vector<FooUnion> parsed_member{};
      {
        auto parsed = DeserializeUnionSet(*member);
        if (!parsed) return std::move(parsed).error();
        parsed_member = std::move(*parsed);
      }
      out.unionList = std::move(parsed_member);
    }
  }
  return out;
}

smithy::Document SerializeMalformedUniqueItemsOutput(const MalformedUniqueItemsOutput& value) {
  smithy::DocumentMap map;
  return smithy::Document(std::move(map));
}

smithy::Outcome<MalformedUniqueItemsOutput> DeserializeMalformedUniqueItemsOutput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("MalformedUniqueItemsOutput: expected a map on the wire");
  MalformedUniqueItemsOutput out;
  return out;
}

smithy::Document SerializeSensitiveValidationInput(const SensitiveValidationInput& value) {
  smithy::DocumentMap map;
  if (value.string.has_value()) {
    map.emplace("string", smithy::Document((*value.string)));
  }
  return smithy::Document(std::move(map));
}

smithy::Outcome<SensitiveValidationInput> DeserializeSensitiveValidationInput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("SensitiveValidationInput: expected a map on the wire");
  SensitiveValidationInput out;
  {
    const smithy::Document* member = doc.Find("string");
    if (member != nullptr && !member->is_null()) {
      std::string parsed_member{};
      if (!member->is_string()) return smithy::Error::Serialization("SensitiveValidationInput.string: unexpected type on the wire");
      parsed_member = member->as_string();
      out.string = std::move(parsed_member);
    }
  }
  return out;
}

smithy::Document SerializeSensitiveValidationOutput(const SensitiveValidationOutput& value) {
  smithy::DocumentMap map;
  return smithy::Document(std::move(map));
}

smithy::Outcome<SensitiveValidationOutput> DeserializeSensitiveValidationOutput(const smithy::Document& doc) {
  if (!doc.is_map()) return smithy::Error::Serialization("SensitiveValidationOutput: expected a map on the wire");
  SensitiveValidationOutput out;
  return out;
}

}  // namespace smithy::protocoltests::restjsonvalidation
