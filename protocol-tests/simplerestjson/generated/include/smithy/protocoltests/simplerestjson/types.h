// Code generated by smithy-cpp (cpp-codegen). DO NOT EDIT.

#pragma once

#include <cstdint>
#include <map>
#include <optional>
#include <string>
#include <string_view>
#include <utility>
#include <variant>
#include <vector>

#include "smithy/core/document.h"
#include "smithy/core/fatal.h"
#include "smithy/core/timestamp.h"

namespace smithy::protocoltests::simplerestjson {

class PizzaBase {
  public:
    enum class Value {
      kCream,
      kTomato,
      kUnknown,
    };

    PizzaBase() = default;
    PizzaBase(Value value) : value_(value) {}  // NOLINT(*-explicit-*)

    /// Unknown wire values are preserved and reported as Value::kUnknown.
    static PizzaBase FromString(std::string_view text) {
      if (text == "C") return PizzaBase(Value::kCream);
      if (text == "T") return PizzaBase(Value::kTomato);
      PizzaBase result;
      result.unknown_ = std::string(text);
      return result;
    }

    Value value() const { return value_; }

    /// The wire text, including the original text of unknown values.
    std::string_view ToString() const {
      switch (value_) {
        case Value::kCream: return "C";
        case Value::kTomato: return "T";
        case Value::kUnknown: return unknown_;
      }
      return unknown_;
    }

    friend bool operator==(const PizzaBase&, const PizzaBase&) = default;

  private:
    Value value_ = Value::kUnknown;
    std::string unknown_;
};


class Ingredient {
  public:
    enum class Value {
      kTomato,
      kCheese,
      kPineapple,
      kBacon,
      kChicken,
      kSalad,
      kMushroom,
      kOlives,
      kOnions,
      kPepperoni,
      kPeppers,
      kUnknown,
    };

    Ingredient() = default;
    Ingredient(Value value) : value_(value) {}  // NOLINT(*-explicit-*)

    /// Unknown wire values are preserved and reported as Value::kUnknown.
    static Ingredient FromString(std::string_view text) {
      if (text == "TOMATO") return Ingredient(Value::kTomato);
      if (text == "CHEESE") return Ingredient(Value::kCheese);
      if (text == "PINEAPPLE") return Ingredient(Value::kPineapple);
      if (text == "BACON") return Ingredient(Value::kBacon);
      if (text == "CHICKEN") return Ingredient(Value::kChicken);
      if (text == "Salad") return Ingredient(Value::kSalad);
      if (text == "MUSHROOM") return Ingredient(Value::kMushroom);
      if (text == "OLIVES") return Ingredient(Value::kOlives);
      if (text == "ONIONS") return Ingredient(Value::kOnions);
      if (text == "PEPPERONI") return Ingredient(Value::kPepperoni);
      if (text == "PEPPERS") return Ingredient(Value::kPeppers);
      Ingredient result;
      result.unknown_ = std::string(text);
      return result;
    }

    Value value() const { return value_; }

    /// The wire text, including the original text of unknown values.
    std::string_view ToString() const {
      switch (value_) {
        case Value::kTomato: return "TOMATO";
        case Value::kCheese: return "CHEESE";
        case Value::kPineapple: return "PINEAPPLE";
        case Value::kBacon: return "BACON";
        case Value::kChicken: return "CHICKEN";
        case Value::kSalad: return "Salad";
        case Value::kMushroom: return "MUSHROOM";
        case Value::kOlives: return "OLIVES";
        case Value::kOnions: return "ONIONS";
        case Value::kPepperoni: return "PEPPERONI";
        case Value::kPeppers: return "PEPPERS";
        case Value::kUnknown: return unknown_;
      }
      return unknown_;
    }

    friend bool operator==(const Ingredient&, const Ingredient&) = default;

  private:
    Value value_ = Value::kUnknown;
    std::string unknown_;
};


struct Pizza {
  std::string name{};
  PizzaBase base{};
  std::vector<Ingredient> toppings{};

  friend bool operator==(const Pizza&, const Pizza&) = default;
};


struct Salad {
  std::string name{};
  std::vector<Ingredient> ingredients{};

  friend bool operator==(const Salad&, const Salad&) = default;
};


class Food {
  public:
    Food() = default;

    static Food FromPizza(Pizza value) {
      Food result;
      result.value_.emplace<1>(std::move(value));
      return result;
    }
    bool is_pizza() const { return value_.index() == 1; }
    const Pizza& as_pizza() const {
      if (!is_pizza()) smithy::internal::FatalWrongUnionAccess("Food", "pizza", case_name());
      return std::get<1>(value_);
    }
    /// The engaged member, or nullptr when another member (or none) is set.
    const Pizza* as_pizza_or_null() const { return std::get_if<1>(&value_); }

    static Food FromSalad(Salad value) {
      Food result;
      result.value_.emplace<2>(std::move(value));
      return result;
    }
    bool is_salad() const { return value_.index() == 2; }
    const Salad& as_salad() const {
      if (!is_salad()) smithy::internal::FatalWrongUnionAccess("Food", "salad", case_name());
      return std::get<2>(value_);
    }
    /// The engaged member, or nullptr when another member (or none) is set.
    const Salad* as_salad_or_null() const { return std::get_if<2>(&value_); }

    /// True until one of the From* factories has been used.
    bool empty() const { return value_.index() == 0; }

    /// Name of the engaged member, "(empty)" until a From* factory has run.
    const char* case_name() const {
      static constexpr const char* kNames[] = {"(empty)", "pizza", "salad"};
      return kNames[value_.index()];
    }

    /// Applies `visitor` to the engaged member. The visitor must also accept
    /// std::monostate, which represents the empty state.
    template <typename Visitor>
    decltype(auto) visit(Visitor&& visitor) const {
      return std::visit(std::forward<Visitor>(visitor), value_);
    }

    friend bool operator==(const Food&, const Food&) = default;

  private:
    std::variant<std::monostate, Pizza, Salad> value_;
};


struct MenuItem {
  Food food{};
  float price{};

  friend bool operator==(const MenuItem&, const MenuItem&) = default;
};


struct AddMenuItemInput {
  std::string restaurant{};
  MenuItem menuItem{};

  friend bool operator==(const AddMenuItemInput&, const AddMenuItemInput&) = default;
};


struct AddMenuItemOutput {
  std::string itemId{};
  smithy::Timestamp added{};

  friend bool operator==(const AddMenuItemOutput&, const AddMenuItemOutput&) = default;
};


struct GenericClientError {
  std::string message{};

  friend bool operator==(const GenericClientError&, const GenericClientError&) = default;
};


struct GenericServerError {
  std::string message{};

  friend bool operator==(const GenericServerError&, const GenericServerError&) = default;
};


struct PriceError {
  std::string message{};
  std::int32_t code{};

  friend bool operator==(const PriceError&, const PriceError&) = default;
};


struct CustomCodeInput {
  std::int32_t code{};

  friend bool operator==(const CustomCodeInput&, const CustomCodeInput&) = default;
};


struct CustomCodeOutput {
  std::optional<std::int32_t> code{};

  friend bool operator==(const CustomCodeOutput&, const CustomCodeOutput&) = default;
};


class UnknownServerErrorCode {
  public:
    enum class Value {
      kErrorCode,
      kUnknown,
    };

    UnknownServerErrorCode() = default;
    UnknownServerErrorCode(Value value) : value_(value) {}  // NOLINT(*-explicit-*)

    /// Unknown wire values are preserved and reported as Value::kUnknown.
    static UnknownServerErrorCode FromString(std::string_view text) {
      if (text == "server.error") return UnknownServerErrorCode(Value::kErrorCode);
      UnknownServerErrorCode result;
      result.unknown_ = std::string(text);
      return result;
    }

    Value value() const { return value_; }

    /// The wire text, including the original text of unknown values.
    std::string_view ToString() const {
      switch (value_) {
        case Value::kErrorCode: return "server.error";
        case Value::kUnknown: return unknown_;
      }
      return unknown_;
    }

    friend bool operator==(const UnknownServerErrorCode&, const UnknownServerErrorCode&) = default;

  private:
    Value value_ = Value::kUnknown;
    std::string unknown_;
};


struct UnknownServerError {
  UnknownServerErrorCode errorCode{};
  std::optional<std::string> description{};
  std::optional<std::string> stateHash{};

  friend bool operator==(const UnknownServerError&, const UnknownServerError&) = default;
};


enum class EnumResult : std::int32_t {
  kFirst = 1,
  kSecond = 2,
};


struct FallbackError {
  std::string error{};

  friend bool operator==(const FallbackError&, const FallbackError&) = default;
};


class TheEnum {
  public:
    enum class Value {
      kV1,
      kV2,
      kUnknown,
    };

    TheEnum() = default;
    TheEnum(Value value) : value_(value) {}  // NOLINT(*-explicit-*)

    /// Unknown wire values are preserved and reported as Value::kUnknown.
    static TheEnum FromString(std::string_view text) {
      if (text == "v1") return TheEnum(Value::kV1);
      if (text == "v2") return TheEnum(Value::kV2);
      TheEnum result;
      result.unknown_ = std::string(text);
      return result;
    }

    Value value() const { return value_; }

    /// The wire text, including the original text of unknown values.
    std::string_view ToString() const {
      switch (value_) {
        case Value::kV1: return "v1";
        case Value::kV2: return "v2";
        case Value::kUnknown: return unknown_;
      }
      return unknown_;
    }

    friend bool operator==(const TheEnum&, const TheEnum&) = default;

  private:
    Value value_ = Value::kUnknown;
    std::string unknown_;
};


struct GetEnumInput {
  TheEnum aa{};

  friend bool operator==(const GetEnumInput&, const GetEnumInput&) = default;
};


struct GetEnumOutput {
  std::optional<std::string> result{};

  friend bool operator==(const GetEnumOutput&, const GetEnumOutput&) = default;
};


struct GetIntEnumInput {
  EnumResult aa{};

  friend bool operator==(const GetIntEnumInput&, const GetIntEnumInput&) = default;
};


struct GetIntEnumOutput {
  EnumResult result{};

  friend bool operator==(const GetIntEnumOutput&, const GetIntEnumOutput&) = default;
};


struct GetMenuInput {
  std::string restaurant{};

  friend bool operator==(const GetMenuInput&, const GetMenuInput&) = default;
};


struct GetMenuOutput {
  std::map<std::string, MenuItem> menu{};

  friend bool operator==(const GetMenuOutput&, const GetMenuOutput&) = default;
};


struct NotFoundError {
  std::string name{};

  friend bool operator==(const NotFoundError&, const NotFoundError&) = default;
};


struct HeaderEndpointInput {
  std::optional<std::string> uppercaseHeader{};
  std::optional<std::string> capitalizedHeader{};
  std::optional<std::string> lowercaseHeader{};
  std::optional<std::string> mixedHeader{};

  friend bool operator==(const HeaderEndpointInput&, const HeaderEndpointInput&) = default;
};


struct HeaderEndpointOutput {
  std::optional<std::string> uppercaseHeader{};
  std::optional<std::string> capitalizedHeader{};
  std::optional<std::string> lowercaseHeader{};
  std::optional<std::string> mixedHeader{};

  friend bool operator==(const HeaderEndpointOutput&, const HeaderEndpointOutput&) = default;
};


struct HealthInput {
  std::optional<std::string> query{};

  friend bool operator==(const HealthInput&, const HealthInput&) = default;
};


struct HealthOutput {
  std::string status{};

  friend bool operator==(const HealthOutput&, const HealthOutput&) = default;
};


struct HttpPayloadRequiredWithDefaultInput {
  std::string body = "default value";

  friend bool operator==(const HttpPayloadRequiredWithDefaultInput&, const HttpPayloadRequiredWithDefaultInput&) = default;
};


struct HttpPayloadRequiredWithDefaultOutput {
  std::string body = "default value";

  friend bool operator==(const HttpPayloadRequiredWithDefaultOutput&, const HttpPayloadRequiredWithDefaultOutput&) = default;
};


struct HttpPayloadWithDefaultInput {
  std::optional<std::string> body{};

  friend bool operator==(const HttpPayloadWithDefaultInput&, const HttpPayloadWithDefaultInput&) = default;
};


struct HttpPayloadWithDefaultOutput {
  std::string body = "default value";

  friend bool operator==(const HttpPayloadWithDefaultOutput&, const HttpPayloadWithDefaultOutput&) = default;
};


struct SmallStruct {
  std::string content{};

  friend bool operator==(const SmallStruct&, const SmallStruct&) = default;
};


class OpenDiscriminatedUnion {
  public:
    OpenDiscriminatedUnion() = default;

    static OpenDiscriminatedUnion FromSmol(SmallStruct value) {
      OpenDiscriminatedUnion result;
      result.value_.emplace<1>(std::move(value));
      return result;
    }
    bool is_smol() const { return value_.index() == 1; }
    const SmallStruct& as_smol() const {
      if (!is_smol()) smithy::internal::FatalWrongUnionAccess("OpenDiscriminatedUnion", "smol", case_name());
      return std::get<1>(value_);
    }
    /// The engaged member, or nullptr when another member (or none) is set.
    const SmallStruct* as_smol_or_null() const { return std::get_if<1>(&value_); }

    static OpenDiscriminatedUnion FromOther(smithy::Document value) {
      OpenDiscriminatedUnion result;
      result.value_.emplace<2>(std::move(value));
      return result;
    }
    bool is_other() const { return value_.index() == 2; }
    const smithy::Document& as_other() const {
      if (!is_other()) smithy::internal::FatalWrongUnionAccess("OpenDiscriminatedUnion", "other", case_name());
      return std::get<2>(value_);
    }
    /// The engaged member, or nullptr when another member (or none) is set.
    const smithy::Document* as_other_or_null() const { return std::get_if<2>(&value_); }

    /// True until one of the From* factories has been used.
    bool empty() const { return value_.index() == 0; }

    /// Name of the engaged member, "(empty)" until a From* factory has run.
    const char* case_name() const {
      static constexpr const char* kNames[] = {"(empty)", "smol", "other"};
      return kNames[value_.index()];
    }

    /// Applies `visitor` to the engaged member. The visitor must also accept
    /// std::monostate, which represents the empty state.
    template <typename Visitor>
    decltype(auto) visit(Visitor&& visitor) const {
      return std::visit(std::forward<Visitor>(visitor), value_);
    }

    friend bool operator==(const OpenDiscriminatedUnion&, const OpenDiscriminatedUnion&) = default;

  private:
    std::variant<std::monostate, SmallStruct, smithy::Document> value_;
};


class OpenTaggedUnion {
  public:
    OpenTaggedUnion() = default;

    static OpenTaggedUnion FromStr(std::string value) {
      OpenTaggedUnion result;
      result.value_.emplace<1>(std::move(value));
      return result;
    }
    bool is_str() const { return value_.index() == 1; }
    const std::string& as_str() const {
      if (!is_str()) smithy::internal::FatalWrongUnionAccess("OpenTaggedUnion", "str", case_name());
      return std::get<1>(value_);
    }
    /// The engaged member, or nullptr when another member (or none) is set.
    const std::string* as_str_or_null() const { return std::get_if<1>(&value_); }

    static OpenTaggedUnion FromOther(smithy::Document value) {
      OpenTaggedUnion result;
      result.value_.emplace<2>(std::move(value));
      return result;
    }
    bool is_other() const { return value_.index() == 2; }
    const smithy::Document& as_other() const {
      if (!is_other()) smithy::internal::FatalWrongUnionAccess("OpenTaggedUnion", "other", case_name());
      return std::get<2>(value_);
    }
    /// The engaged member, or nullptr when another member (or none) is set.
    const smithy::Document* as_other_or_null() const { return std::get_if<2>(&value_); }

    /// True until one of the From* factories has been used.
    bool empty() const { return value_.index() == 0; }

    /// Name of the engaged member, "(empty)" until a From* factory has run.
    const char* case_name() const {
      static constexpr const char* kNames[] = {"(empty)", "str", "other"};
      return kNames[value_.index()];
    }

    /// Applies `visitor` to the engaged member. The visitor must also accept
    /// std::monostate, which represents the empty state.
    template <typename Visitor>
    decltype(auto) visit(Visitor&& visitor) const {
      return std::visit(std::forward<Visitor>(visitor), value_);
    }

    friend bool operator==(const OpenTaggedUnion&, const OpenTaggedUnion&) = default;

  private:
    std::variant<std::monostate, std::string, smithy::Document> value_;
};


class OpenUnionsPayload {
  public:
    OpenUnionsPayload() = default;

    static OpenUnionsPayload FromTagged(OpenTaggedUnion value) {
      OpenUnionsPayload result;
      result.value_.emplace<1>(std::move(value));
      return result;
    }
    bool is_tagged() const { return value_.index() == 1; }
    const OpenTaggedUnion& as_tagged() const {
      if (!is_tagged()) smithy::internal::FatalWrongUnionAccess("OpenUnionsPayload", "tagged", case_name());
      return std::get<1>(value_);
    }
    /// The engaged member, or nullptr when another member (or none) is set.
    const OpenTaggedUnion* as_tagged_or_null() const { return std::get_if<1>(&value_); }

    static OpenUnionsPayload FromDiscriminated(OpenDiscriminatedUnion value) {
      OpenUnionsPayload result;
      result.value_.emplace<2>(std::move(value));
      return result;
    }
    bool is_discriminated() const { return value_.index() == 2; }
    const OpenDiscriminatedUnion& as_discriminated() const {
      if (!is_discriminated()) smithy::internal::FatalWrongUnionAccess("OpenUnionsPayload", "discriminated", case_name());
      return std::get<2>(value_);
    }
    /// The engaged member, or nullptr when another member (or none) is set.
    const OpenDiscriminatedUnion* as_discriminated_or_null() const { return std::get_if<2>(&value_); }

    /// True until one of the From* factories has been used.
    bool empty() const { return value_.index() == 0; }

    /// Name of the engaged member, "(empty)" until a From* factory has run.
    const char* case_name() const {
      static constexpr const char* kNames[] = {"(empty)", "tagged", "discriminated"};
      return kNames[value_.index()];
    }

    /// Applies `visitor` to the engaged member. The visitor must also accept
    /// std::monostate, which represents the empty state.
    template <typename Visitor>
    decltype(auto) visit(Visitor&& visitor) const {
      return std::visit(std::forward<Visitor>(visitor), value_);
    }

    friend bool operator==(const OpenUnionsPayload&, const OpenUnionsPayload&) = default;

  private:
    std::variant<std::monostate, OpenTaggedUnion, OpenDiscriminatedUnion> value_;
};


struct OpenUnionsInput {
  OpenUnionsPayload data{};

  friend bool operator==(const OpenUnionsInput&, const OpenUnionsInput&) = default;
};


struct OpenUnionsOutput {
  OpenUnionsPayload data{};

  friend bool operator==(const OpenUnionsOutput&, const OpenUnionsOutput&) = default;
};


struct RoundTripInput {
  std::string label{};
  std::optional<std::string> header{};
  std::optional<std::string> query{};
  std::optional<std::string> body{};

  friend bool operator==(const RoundTripInput&, const RoundTripInput&) = default;
};


struct RoundTripOutput {
  std::string label{};
  std::optional<std::string> header{};
  std::optional<std::string> query{};
  std::optional<std::string> body{};

  friend bool operator==(const RoundTripOutput&, const RoundTripOutput&) = default;
};


struct VersionInput {
  friend bool operator==(const VersionInput&, const VersionInput&) = default;
};


struct VersionOutput {
  std::string version{};

  friend bool operator==(const VersionOutput&, const VersionOutput&) = default;
};

}  // namespace smithy::protocoltests::simplerestjson
