// Code generated by smithy-cpp (cpp-codegen). DO NOT EDIT.

#include <cstdint>
#include <gtest/gtest.h>
#include <limits>
#include <memory>
#include <optional>
#include <string>
#include <utility>

#include "smithy/protocoltests/simplerestjson/client.h"
#include "smithy/testing/protocol_test.h"

namespace smithy::protocoltests::simplerestjson {

// Generated from smithy.test#httpResponseTests (client cases),
// including the cases attached to modeled error shapes.
//
// Excluded cases (protocol-test-exclusions.txt; the list must only shrink):
//   CustomCodeOutput (response) — 3xx @httpResponseCode is not treated as a success status
//   GetEnumOutput (response) — under investigation (task #63): enum output server-side 400
//   OpenUnionsUnknownTaggedUnionCase (response) — alloy open/discriminated unions are not implemented
//   OpenUnionsKnownDiscriminatedUnionCase (response) — alloy open/discriminated unions are not implemented
//   OpenUnionsUnknownDiscriminatedUnionCase (response) — alloy open/discriminated unions are not implemented
//   RoundTripDataResponse (response) — under investigation (task #63): mixed label/query/body echo

namespace {

struct Fixture {
  std::shared_ptr<smithy::testing::CapturingTransport> transport;
  PizzaAdminServiceClient client;
};

Fixture MakeFixture(const std::string& endpoint = "") {
  auto transport = std::make_shared<smithy::testing::CapturingTransport>();
  smithy::ClientConfig config;
  config.retry.max_attempts = 1;  // wire-exact tests: no retries
  config.http_client = transport;
  config.endpoint = endpoint;
  // Create cannot fail when a transport is injected.
  auto client = PizzaAdminServiceClient::Create(std::move(config));
  return Fixture{std::move(transport), *std::move(client)};
}

}  // namespace

// add menu item response tests
TEST(PizzaAdminServiceResponseTest, AddMenuItemResult) {
  Fixture fixture = MakeFixture();
  fixture.transport->next_response.status = 201;
  fixture.transport->next_response.headers.Set("Content-Type", "application/json");
  fixture.transport->next_response.headers.Set("X-ADDED-AT", "1576540098");
  fixture.transport->next_response.body = "\"1\"";
  const auto outcome = fixture.client.AddMenuItem(AddMenuItemInput{});
  ASSERT_TRUE(outcome.ok()) << outcome.error().message();
  const AddMenuItemOutput expected = [] {
  AddMenuItemOutput v{};
  v.itemId = "1";
  v.added = smithy::Timestamp::FromEpochMilliseconds(1576540098000LL);
  return v;
}();
  EXPECT_EQ(*outcome, expected);
}

TEST(PizzaAdminServiceResponseTest, GetIntEnumOutput) {
  Fixture fixture = MakeFixture();
  fixture.transport->next_response.status = 200;
  fixture.transport->next_response.body = "{\"result\":1}";
  const auto outcome = fixture.client.GetIntEnum(GetIntEnumInput{});
  ASSERT_TRUE(outcome.ok()) << outcome.error().message();
  const GetIntEnumOutput expected = [] {
  GetIntEnumOutput v{};
  v.result = static_cast<EnumResult>(1);
  return v;
}();
  EXPECT_EQ(*outcome, expected);
}

TEST(PizzaAdminServiceResponseTest, GetMenuResponse) {
  Fixture fixture = MakeFixture();
  fixture.transport->next_response.status = 200;
  fixture.transport->next_response.body = "{\"a0b0f3a9-81d3-4bf3-8897-a76423116403\" :{\"food\":{\"pizza\": {\"name\":\"margharita\",\"base\":\"T\",\"toppings\":[\"MUSHROOM\", \"TOMATO\"]}},\"price\":9.0}}\n";
  const auto outcome = fixture.client.GetMenu(GetMenuInput{});
  ASSERT_TRUE(outcome.ok()) << outcome.error().message();
  const GetMenuOutput expected = [] {
  GetMenuOutput v{};
  v.menu = std::map<std::string, MenuItem>{{"a0b0f3a9-81d3-4bf3-8897-a76423116403", [] {
  MenuItem v{};
  v.food = Food::FromPizza([] {
  Pizza v{};
  v.name = "margharita";
  v.base = PizzaBase::FromString("T");
  v.toppings = std::vector<Ingredient>{Ingredient::FromString("MUSHROOM"), Ingredient::FromString("TOMATO")};
  return v;
}());
  v.price = 9.0F;
  return v;
}()}};
  return v;
}();
  EXPECT_EQ(*outcome, expected);
}

// tests variety of casing scenarios for reading http headers
TEST(PizzaAdminServiceResponseTest, headerEndpointResponse) {
  Fixture fixture = MakeFixture();
  fixture.transport->next_response.status = 200;
  fixture.transport->next_response.headers.Set("X-Capitalized-Header", "Capitalized_value");
  fixture.transport->next_response.headers.Set("X-UPPERCASE-HEADER", "UPPERCASE_VALUE");
  fixture.transport->next_response.headers.Set("x-MiXeD-hEaDEr", "aLLMiXedUP");
  fixture.transport->next_response.headers.Set("x-lowercase-header", "lowercase_value");
  fixture.transport->next_response.body = "";
  const auto outcome = fixture.client.HeaderEndpoint(HeaderEndpointInput{});
  ASSERT_TRUE(outcome.ok()) << outcome.error().message();
  const HeaderEndpointOutput expected = [] {
  HeaderEndpointOutput v{};
  v.uppercaseHeader = "UPPERCASE_VALUE";
  v.capitalizedHeader = "Capitalized_value";
  v.lowercaseHeader = "lowercase_value";
  v.mixedHeader = "aLLMiXedUP";
  return v;
}();
  EXPECT_EQ(*outcome, expected);
}

// Pass JSON string value as is if payload provided
TEST(PizzaAdminServiceResponseTest, SimpleRestJsonSomeRequiredHttpPayloadWithDefault) {
  Fixture fixture = MakeFixture();
  fixture.transport->next_response.status = 200;
  fixture.transport->next_response.headers.Set("Content-Type", "application/json");
  fixture.transport->next_response.body = "\"custom value\"";
  const auto outcome = fixture.client.HttpPayloadRequiredWithDefault(HttpPayloadRequiredWithDefaultInput{});
  ASSERT_TRUE(outcome.ok()) << outcome.error().message();
  const HttpPayloadRequiredWithDefaultOutput expected = [] {
  HttpPayloadRequiredWithDefaultOutput v{};
  v.body = "custom value";
  return v;
}();
  EXPECT_EQ(*outcome, expected);
}

// Use default value when there is no payload
TEST(PizzaAdminServiceResponseTest, SimpleRestJsonNoneRequiredHttpPayloadWithDefault) {
  Fixture fixture = MakeFixture();
  fixture.transport->next_response.status = 200;
  fixture.transport->next_response.body = "";
  const auto outcome = fixture.client.HttpPayloadRequiredWithDefault(HttpPayloadRequiredWithDefaultInput{});
  ASSERT_TRUE(outcome.ok()) << outcome.error().message();
  const HttpPayloadRequiredWithDefaultOutput expected = [] {
  HttpPayloadRequiredWithDefaultOutput v{};
  v.body = "default value";
  return v;
}();
  EXPECT_EQ(*outcome, expected);
}

// Pass JSON string value as is if payload provided
TEST(PizzaAdminServiceResponseTest, SimpleRestJsonSomeHttpPayloadWithDefault) {
  Fixture fixture = MakeFixture();
  fixture.transport->next_response.status = 200;
  fixture.transport->next_response.headers.Set("Content-Type", "application/json");
  fixture.transport->next_response.body = "\"custom value\"";
  const auto outcome = fixture.client.HttpPayloadWithDefault(HttpPayloadWithDefaultInput{});
  ASSERT_TRUE(outcome.ok()) << outcome.error().message();
  const HttpPayloadWithDefaultOutput expected = [] {
  HttpPayloadWithDefaultOutput v{};
  v.body = "custom value";
  return v;
}();
  EXPECT_EQ(*outcome, expected);
}

// Use default value when there is no payload
TEST(PizzaAdminServiceResponseTest, SimpleRestJsonNoneHttpPayloadWithDefault) {
  Fixture fixture = MakeFixture();
  fixture.transport->next_response.status = 200;
  fixture.transport->next_response.body = "";
  const auto outcome = fixture.client.HttpPayloadWithDefault(HttpPayloadWithDefaultInput{});
  ASSERT_TRUE(outcome.ok()) << outcome.error().message();
  const HttpPayloadWithDefaultOutput expected = [] {
  HttpPayloadWithDefaultOutput v{};
  v.body = "default value";
  return v;
}();
  EXPECT_EQ(*outcome, expected);
}

// Return a known tagged union value in an open union
TEST(PizzaAdminServiceResponseTest, OpenUnionsKnownTaggedUnionCase) {
  Fixture fixture = MakeFixture();
  fixture.transport->next_response.status = 200;
  fixture.transport->next_response.headers.Set("Content-Type", "application/json");
  fixture.transport->next_response.body = "{\"tagged\": {\"str\": \"string value\"}}";
  const auto outcome = fixture.client.OpenUnions(OpenUnionsInput{});
  ASSERT_TRUE(outcome.ok()) << outcome.error().message();
  const OpenUnionsOutput expected = [] {
  OpenUnionsOutput v{};
  v.data = OpenUnionsPayload::FromTagged(OpenTaggedUnion::FromStr("string value"));
  return v;
}();
  EXPECT_EQ(*outcome, expected);
}

TEST(PizzaAdminServiceResponseTest, VersionOutput) {
  Fixture fixture = MakeFixture();
  fixture.transport->next_response.status = 200;
  fixture.transport->next_response.body = "\"1.0\"";
  const auto outcome = fixture.client.Version(VersionInput{});
  ASSERT_TRUE(outcome.ok()) << outcome.error().message();
  const VersionOutput expected = [] {
  VersionOutput v{};
  v.version = "1.0";
  return v;
}();
  EXPECT_EQ(*outcome, expected);
}

TEST(PizzaAdminServiceErrorTest, NotFoundError) {
  Fixture fixture = MakeFixture();
  fixture.transport->next_response.status = 404;
  fixture.transport->next_response.headers.Set("Content-Type", "application/json");
  fixture.transport->next_response.headers.Set("X-Error-Type", "NotFoundError");
  fixture.transport->next_response.body = "{\"name\":\"unknown\"}\n";
  const auto outcome = fixture.client.GetMenu(GetMenuInput{});
  ASSERT_FALSE(outcome.ok());
  EXPECT_EQ(outcome.error().code(), "NotFoundError");
  const auto* detail = outcome.error().detail<NotFoundError>();
  ASSERT_NE(detail, nullptr);
  const NotFoundError expected = [] {
  NotFoundError v{};
  v.name = "unknown";
  return v;
}();
  EXPECT_EQ(*detail, expected);
}

// the payload produced on price error
TEST(PizzaAdminServiceErrorTest, PriceErrorTest) {
  Fixture fixture = MakeFixture();
  fixture.transport->next_response.status = 400;
  fixture.transport->next_response.headers.Set("Content-Type", "application/json");
  fixture.transport->next_response.headers.Set("X-CODE", "400");
  fixture.transport->next_response.body = "{\"message\":\"Price must be greater than 0\"}";
  const auto outcome = fixture.client.AddMenuItem(AddMenuItemInput{});
  ASSERT_FALSE(outcome.ok());
  EXPECT_EQ(outcome.error().code(), "PriceError");
  const auto* detail = outcome.error().detail<PriceError>();
  ASSERT_NE(detail, nullptr);
  const PriceError expected = [] {
  PriceError v{};
  v.message = "Price must be greater than 0";
  v.code = 400;
  return v;
}();
  EXPECT_EQ(*detail, expected);
}

}  // namespace smithy::protocoltests::simplerestjson
